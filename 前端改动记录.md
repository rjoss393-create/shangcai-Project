# 前端改动记录

> 由控制层/整合方维护：每次修改 前端/ 目录下的文件都在此追加一条记录，
> 供前端负责人同步合并。commit message 中亦会点明前端改动。

## 2026-09-14 晚：整合前端最新版 + 视频接入 + 字段统一

### 1. 合并前端最新版（来源：前端/SUFE-Knowledge-Galaxy(新)/，由前端负责人交付）
- `index.html`：整体替换为最新版（新增"学习资料"视频区、科研论文区、书籍详情小图 bookGraph），
  随后**重新加回**"当前图谱"下拉框（galaxy-tabs 内右侧，5 项：econ/corp_fin/intl_inv/invest/ma）——
  新版的 index.html 没有下拉框，是从旧版移植回来的。
- `css/style.css`：整体替换为最新版（含 study/paper/book-graph 等新样式）。
- `js/main.js`：整体替换为最新版（94KB，含学习资料视频区逻辑 + 书详情小图逻辑）。
- `js/galaxy.js` / `js/assistant.js`：**未替换**，保留我们的 graph_id 集成版本
  （新版这两个文件是旧基线上改的，不含 graph_id 集成，若直接替换会丢功能）。
- 删除 前端/SUFE-Knowledge-Galaxy/backend/（旧版 controller + mock_main.py 副本，此前已清理过一次，又随新包出现）。
- `data/videos.json`、`assets/videos/`：由脚本生成（见第 3 条）。

### 2. 字段名统一 name/category → label/type（与后端《字段.md》契约一致）
- `js/galaxy.js`：
  - 节点文字/悬停标题/弹窗标题 4 处 `d.name` → `d.label`
  - 新增 `categoryOf(node)`：`node.category` 存在则兼容旧值；否则按 `node.type` 映射
    （chapter/section→"课程"、concept→"概念"、其余空）；`classOf`/`ringOf` 改用 categoryOf。
    效果：章/节节点进入内环（RING_RADIUS 155）且宏观层红色突出，概念中环（250），公式外环（335）。
- `js/main.js`（书详情小图）：
  - 节点文字 `d.name` → `d.label`（2 处）
  - `fetchBookGraph` 已接真实后端 `/api/graph/book/{bookId}`（返回宏观章层小图），
    失败时降级为示例数据；后端数据节点补 `r: 14` 供渲染。
- `css/style.css`：为书详情小图按 type 上色新增规则
  `.bg-node.chapter/.section/.concept/.formula circle`（真实数据此前无对应样式，圆点为默认黑色）。

### 3. 学习资料区视频接入（45 个课程视频）
- `scripts/import_videos.py` 从 知识图谱和书和视频/ 复制并编号：
  金融理论 Andrew Lo P1-P23 → 01-23.mp4；耶鲁公开课 Shiller → 24-45.mp4（源缺 P8，编号连续）。
- `data/videos.json`：45 条清单（title/author/tags/desc 从文件名解析），carousel=[1,3,7,11,14]。
  原前端负责人放的 3 条示例条目（上财投资学导论等）被替换。
- `assets/videos/01.mp4`（前端负责人的示例视频）被覆盖为金融理论第 1 讲；
  `01-cover.png` 未动（若与视频不符需前端负责人确认替换）。
- 视频文件不走 git（.gitignore 已加 *.mp4），由 proxy-server 静态托管；
  ⚠️ 需确认 proxy-server 支持 HTTP Range 请求，否则视频无法拖动进度条。

### 4. 其余
- `js/main.js` BOOKS 数组前 5 项填入 4 本书 + 金融理论课程的真实元数据（此前回退丢失，重新补回）。

### 5. 书籍封面 + 小图提示（用户测试反馈后修复）
- `assets/books/`：旧的 33 张微信截图（9-12 版）删除，用 知识图谱和书和视频/封面/ 下的 33 张新封面
  按文件名顺序重命名为 03.png~35.png（01/02.png 保留）。35 本封面全部可显示。
  ⚠️ 03~35 与书籍的对应关系是按文件名字典序假设的（截图顺序=书单顺序），如有错位需人工调换文件名。
- `index.html`：书详情小图提示文案由"示例图谱结构 · 待接入真实数据"改为
  "本书知识图谱 · 宏观层（章节点）"——该区域现在由后端 /api/graph/book/{id} 返回宏观章层数据。

### 6. 节点类型值与前端样式类冲突的解决方案（type 值域不变量 + 前端映射）
- 冲突：后端《字段.md》契约 type = concept/chapter/section/formula（Agent 层检索依赖，不可改）；
  前端书详情小图样式类为 center/primary/secondary。
- 解决：**契约值域保持不动，前端渲染时做映射**（js/main.js 新增 TYPE_TO_CLS + bgClassOf）：
  chapter→center（核心红）、section→primary（白底红边）、concept/formula→secondary（灰）。
  兼容：若 type 本身就是 center/primary/secondary（示例数据）则原样使用。
- 同步删除此前为真实数据临时加的 .bg-node.chapter/.section/.concept/.formula CSS 规则（映射后不再需要）。
- 请前端负责人知悉此映射约定；星系图（galaxy）维持 course/concept/other 类名体系不受影响。

### 7. 七色节点配色 + 书详情小图修复（用户测试反馈）
- **书详情小图没字/没颜色的根因**：`BOOK_GRAPH_API` 原来是相对路径 `/api/graph/book`，
  页面托管在 3000 端口，请求打到 proxy-server（其只有 /api/kline，没有 /api/graph 代理）→ 404 →
  前端降级画示例图（示例节点无文字、颜色单一）。已改为绝对地址
  `http://localhost:8000/api/graph/book`（与 galaxy.js 的 API_BASE 一致）。
- **七色配色（红橙黄绿青蓝紫，按 id 哈希稳定分配）**：
  - `js/galaxy.js`：新增 NODE_COLORS + colorIndexOf，节点圆 fill 用内联 style 覆盖 CSS 类别色
  - `js/main.js`（书详情小图）：同样新增 NODE_COLORS + nodeColorOf，circle 内联 fill 七色
  - 注：center/primary/secondary 的 CSS 类别色保留但被七色内联色覆盖；
    文字标签颜色不变（深灰 #4a4a4a，center 类白字对七色圆点可能对比不足，待前端负责人细化）。

### 8. 书详情小图深色背景改造（用户反馈白底白字看不清）
- `.book-graph` 背景由白色改为深色 #141a26（与星系主题一致）+ 红色径向光晕；
- 配套调整：`.bg-label` 文字 #4a4a4a → #c9d1d9（浅灰）；`.bg-label.center` 保持白色；
  `.bg-link` 边线改浅灰；`.book-graph-hint`/`.book-graph-loading` 改深色底浅色字。
- 七色节点在深色背景上对比良好。

### 9. 视野裁剪 + 布局分散（性能优化，用户 2026-09-14 深夜提出，联调前留档）
全部改动在 `js/galaxy.js`，**后端契约零改动**：
- **三级结构**：`allNodes/allEdges`（全量数据缓存，含布局坐标）→ `nodes/edges`（活动集=视野+1200 世界单位缓冲，参与力导向，即"附近缓存"）→ `renderNodes/renderEdges`（渲染集=视野+300 缓冲，真正挂 DOM）。
  超出活动缓冲的节点从活动集移除（DOM 与力导向都不再参与），仅保留在全量缓存，平移回来时按原坐标恢复，不发网络请求。
- **裁剪时机**：applyTransform（平移/缩放/旋转实时）+ 力导向每 15 tick 节流重算；render 只对渲染集做 data-join。
- **布局更分散**：link distance 110→175、charge -420→-560、collide 间距 +26→+46、link strength 0.08→0.05。
- **顺带修复"图谱一直动"**：原 render() 每次都会 alpha(0.6).restart()，任何动作（聚焦/高亮）都会让全图重新运动几十秒；现改为只在首次建图（alpha 默认 1）与合并新数据时（alpha 0.3）重启。
- **其他适配**：degree 预计算（degree 基于全量边，裁剪后半径稳定）；findPath/时间轴/漫游/聚焦目标查找改用 allNodes；focus/highlight/选中/路径节点强制进渲染集（后端 actions 可指向视野外节点）。
- **待联调验证点**：① 经济综合 5.4k 节点在默认中观视野下是否流畅；② 平移时边缘补入是否无白屏/闪烁；
  ③ 七色配色+裁剪后 force 参数是否还要微调；④ 与后端 actions（focus/highlight）联动是否正常。

### 10. 视野裁剪首版 bug 修复（用户反馈"图谱看不到"，debug 显示 active/rendered=0）
- **根因**：力导向参数调过头（斥力 -560 等），节点散开直径超过默认视野 + 缓冲带，
  视野中心空白 → 裁剪把节点全部滤出渲染集。
- **修复**：① 力导向参数回调（charge -460 / collide +34 / link 150 / strength 0.06）；
  ② 新增 `fitViewToContent()`：按全量节点包围盒计算缩放+居中（替代原"fit 回中观层"的写死行为），
  后端 zoom fit action 与首次载入布局稳定后（alpha<0.06）各触发一次；
  ③ updateCulling 增加画布尺寸退化兜底（0/NaN 时全量渲染）；
  ④ 新增 `GalaxyEngine.debug()` 输出裁剪三级数量，便于排查。

### 11. 修复概率性"图谱加载不出来"（d3 力导向时序异常）
- **根因**：updateCulling/runSimulation 在活动集缩小时**先 simulation.nodes(新) 后换 links**，
  d3 的 nodes() 会用旧 links 重新初始化力；旧 links 指向刚被移出活动集的节点 → tick 中抛异常 →
  模拟器死亡 → 自动适配视野不再触发 → 视野停在原点、节点全在视野外 → active/rendered=0。
  节点何时跨界取决于时序，所以是概率性的。
- **修复**：两处都改为**先 force('link').links(新) 再 simulation.nodes(新)**；另加 6 秒兜底定时器，
  首次载入后若布局未触发自动适配则强制 fitViewToContent。

### 12. 修复大图谱（corp_fin/intl_inv/econ 等）始终空白
- **根因**：无约束的力导向会把 5437 节点级图谱散到半径数千世界单位，超过 SCALE_MIN(0.28)
  视野能容纳的范围，且环形布局中心有空洞 → 适配后视野停在空洞/圈外 → 全空白。
  ma（419 节点）散得小所以"概率可见"。
- **修复**：① 新增 `confineForce` 围墙力：布局最大半径 1400 世界单位（超界减速 + 严重越界钳位），
  保证任何缩放下都装得进视野；② 首次载入适配改为**跟随式**：布局推进期间每 40 tick 适配一次、
  alpha<0.06 稳定后最终适配，6 秒兜底定时器保留；③ 用户 pointerdown/滚轮介入即停止自动适配，
  避免抢用户视角。

### 13. ⚠️ 未解决：视野裁剪后图谱仍不可见（2026-09-14 深夜暂停，次日继续）
- **现象**：用户强刷测试后仍看不到图谱（大图谱始终空白，ma 概率可见）。
- **已排除/已修的**：力导向参数过散（已回调）、fit 写死中观层（已改内容包围盒）、
  画布尺寸退化（已兜底）、d3 links/nodes 同步顺序（已修）、无界布局散出视野（已加 confine 墙 1400）。
- **明日排查步骤（按顺序）**：
  1. 让用户 Ctrl+F5 / 无痕窗口，排除浏览器缓存旧 JS；
  2. F12 Console 抓红色报错全文（封面 404 除外）；
  3. `GalaxyEngine.debug()` 完整输出（重点看 view: {tx,ty,scale,rot} 和三级数量）：
     - allNodes=0 → 接口/数据问题；rendered>0 但看不见 → transform/CSS 问题；
     - rendered=0 但 active>0 → 裁剪/视野计算问题（重点怀疑 worldViewport 的符号/坐标）；
  4. 检查 load 后 actions 的 fade_in/zoom 与裁剪渲染的时序。
- **兜底方案**：若明日排查不顺利，回退视野裁剪系列（5 个提交
  428b484/0e19b7d/5a11eb0/eec4d39/3166e57），恢复"无裁剪但可见"的版本再分步重做。

### 14. ✅ 第 13 条已定位并修复：根因是节点初始坐标全部重合导致力导向数值爆炸（2026-09-15）
- **排查方式换法**：不再靠猜，用无头 Edge + CDP 直连真实页面测量（真浏览器 + 真后端 + 真数据），
  并用 Node 直接跑 d3 离线复现力导向行为，拿到硬数据再改。
- **实测证据（修复前）**：econ 全量节点世界坐标范围 **±3,700,000**（ma ±14,000），
  `active=0 / rendered=0 / dom=0` —— 布局早被炸飞，与裁剪/视野计算无关。
- **根因**：`mergeGraph` 给新节点的初始坐标写死 `x:0, y:0`（前端原始交付代码就有），首次载入时
  5437 个节点全部重合在原点；d3 四叉树斥力在零距离处数值爆炸——离线实测
  **第 1 tick 半径 146 万、第 10 tick 1.35 亿世界单位**。节点飞出视野后被裁剪移出活动集，
  坐标永久冻结在百万级 → 图谱永久空白；爆炸程度随时序不同 → 表现为"概率可见"。
  裁剪只是把一次可恢复的抖动画成了永久空白，放大了这个原有缺陷。
- **修复（js/galaxy.js，2 处）**：
  1. 新节点改用 d3 同款费马螺旋撒点 `r = 10*sqrt(0.5+i), a = i*π*(3-√5)`
     （离线实测：同参数下半径稳定在 ~2277，不再爆炸）；
  2. `SCALE_MIN` 0.28 → 0.12：最大图谱（econ 跨度 ~4500）适配需 ~0.14，原下限把
     `fitViewToContent` 卡死、装不进视野，这是第二个独立缺陷。
- **验证（无头真实浏览器）**：
  | 检查 | 修复前 | 修复后 |
  |---|---|---|
  | econ 世界坐标范围 | ±3,700,000 | ±1,694 |
  | econ rendered / 屏内 | 0 / 0 | 5437 / 5437（scale 0.191） |
  | ma rendered / 屏内 | 0 / 0 | 419 / 385（scale 0.369） |
  | 点节点 → 后端 actions | — | 正常，focused 生效，无异常 |
  | ma→econ→ma 反复切书 | — | 419/5437/419 全对，无 console 异常 |
- 宏观层覆盖度：econ 在总览缩放下圆点重叠较密（≈108% 覆盖），属 5437 节点全图总览的固有效果
  （CSS 已按"宏观=星云 + 只留核心简称"设计）；放大到中观/微观层即恢复清晰。
- confine 墙（3166e57）经实测**确实有效**（去掉后 econ 跨度 5847），保留；仅订正注释里
  "保证任何缩放下都装得进视野"的过强表述。

### 15. 分层显示改造：按数据层级过滤渲染集（宏观=章 / 中观=章+节 / 微观=全部）（2026-09-15）
- **背景**：用户反馈"节点显示非常混乱，看不到自己想看的内容"。实测确认两级原因：
  ① 渲染层：**任何缩放级别都画全量节点**，层级只改圆半径/透明度/标签显隐（宏观 scale 0.3 实测屏内 3998 点 + 1233 标签，其中 1093 个是"节"）；
  ② 数据层：章节点重复（同一章页眉抽成 12 个）、表格数字变成"节"、版权页信息变成"概念"。
- **本次前端改动（js/galaxy.js + css/style.css）**：
  1. **按 `layer` 过滤渲染集**：宏观只渲染 `layer=macro`（章），中观加 `meso`（节），微观全量；
     活动集（参与力导向）仍保持全量，保证放大到深层时下级节点已在正确位置，不会挤成一团。
  2. **边按层级过滤**：`hier`（父子）与 `macro` 恒显，`meso`/`micro` 相关边只在各自层级出现（避免宏观层被几千条"概念-概念"边糊成毛线球）。
  3. **环形半径改为按层级**：macro 500 / meso 800 / micro 1100（原按 category 155/250/335，层级语义没体现）；confine 围墙 1400→1500。
  4. **fitViewToContent 只按当前层级可见节点适配**，并把结果**夹到该层级的缩放带**（macro [0.12,0.4] / meso [0.4,0.8] / micro [0.8,2.0]）——否则"适配本层内容 → 缩放跨层 → 渲染集又变"会来回抖动。
  5. **标签字号按层级补偿**（CSS）：宏观 30px、中观 20px（世界坐标，乘缩放后 ≈12px/8px 屏上可读）；宏观层节点 ≤30 个时显示全名（原来一律截断成 4 字）。
  6. 删除两处死代码：`cls += ' hidden'`（CSS 无此规则，原意"中观隐藏外环文字"从未生效）、`ring-core`（阈值 140 小于最小环半径 155，永不命中）。
  7. **`quality=low` 的短概念标签默认隐藏，hover 才显示**（配合数据层清洗标记，见下）。
- **配套数据层改动**（scripts/generate_layered_data.py，非前端）：新增清洗阶段 + `--only/--dry-run` 参数，
  删除表格数字/正文句子碎片/版权页字段、修标题自重复、从"节号+标题"推导缺失的章；产出《数据清洗报告.md》供审阅。**先只对"并购与重组"(ma) 应用**。
- **验证（无头 Edge + CDP 真实页面，ma 清洗后 161 节点）**：
  | 层级 | scale | 渲染/屏内节点 | 标签 | 构成 |
  |---|---|---|---|---|
  | 宏观 | 0.40 | 6 / 6 | 6 | 全部章节点 |
  | 中观 | 0.53 | 32 / 32 | 32 | 节 26 + 章 6 |
  | 微观 | 0.94 | 63 / 60 | 34 | 概念 42 + 节 14 + 公式 1 + 章 3 |
  微观标签 34 < 屏内 60 = 短概念标签隐藏生效；切书 econ 无异常、无 console 报错。
- **待办**：① ma 的章名是占位（第1/2/3/5/7/8 章，抽取时章标题已丢失，需人工补 8 行，缺第4/6章）；② 其余 4 本书的数据清洗未做（宏观层会显示重复章名，属已知）；③ 层级切换时新旧节点是"直接出现/消失"，未做淡入淡出过渡。

### 16. 修复"宏观↔中观切换后节点全乱" + 滚轮改为光标锚点缩放 + 接入新版 ma 数据（2026-09-15）
- **用户反馈**："从宏观层进入中观层后再回到宏观层数据就全乱了"。
- **根因（实测定位）**：节点坐标原先只在 `ticked()`（布局推进时）写入 DOM。
  布局冷却（alpha 归零、模拟器停止）后再切换层级，**新进入 DOM 的节点拿不到 transform，全部堆在画布中心**。
  实测：ma 进中观 29 个节点里 23 个无坐标、进微观 51 个里 45 个无坐标；econ 进中观 632 个里 471 个无坐标。
  层级过滤之前所有层级都渲染全量节点、极少新增，所以这个老 bug 一直没暴露。
- **修复**：① `render()` 里对合并后的节点立即按数据坐标写 `transform`；② 抽出 `updateEdgeGeometry()`
  供 `ticked()` 与 `render()` 共用，新进入 DOM 的边也立刻算出路径（否则是空路径不显示）。
  修复后复测：econ/ma 各层级 `noTransform` 全部为 0。
- **顺带改**：滚轮缩放由"画布中心锚点"改为**光标锚点**——原实现瞄着某个节点放大会跑偏，
  层级过滤后各层节点分布在不同半径，中心放大会看到空区。实测验：瞄"第1章"连续放大，该节点屏幕位置不变（956→957px）。
- **数据**：用户提供新版《并购与重组_知识图谱》（147 节点，8 个带标题的章、44 节、92 概念，无句子碎片），
  旧版留在 `数据/并购与重组_知识图谱(旧版20260913).json`；换版后重跑分层（138 节点：8 章 / 44 节 / 86 微观），
  并清掉 `backend/cache/ma_all-MiniLM-L6-v2/` 旧 embedding 缓存（节点 id 已变）。

### 17. 微观层点击节点后只显示邻域（无关节点不再显示）（2026-09-15）
- **用户反馈**："微观层点击一个节点后，与它没有连接的节点也会显示，影响观感"。
- **根因**：后端点击时返回 `fade_out`（无关节点进 `state.visibleIds` 黑名单 → 加 `.faded` 类），
  而 CSS 里 `.g-node.faded{opacity:0}` 被另一条**优先级更高**的规则压住：
  `.galaxy-stage.has-focus .g-node.zoom-micro:not(.focused):not(.highlighted):not(.path-node){opacity:.32}`
  —— 微观层一旦有焦点，被淡出的无关节点反而以 32% 不透明度显示出来。
- **修复（css/style.css + js/galaxy.js）**：
  1. 该规则加 `:not(.faded)`（`has-focus` 与非 `has-focus` 两条都加），让"已淡出"保持隐藏；
  2. `handleResponse` 中当后端返回的是**子图**（节点数 < 全量）时，把返回的邻域节点标为 `highlighted`，
     让"真正连着的节点"一眼可辨（否则邻域和其它节点一样是暗的）；
  3. 点击空白处改为**退出"只看邻域"**：清路径/焦点/高亮 + 恢复全量可见
     （原来只清路径，隐藏后无法恢复，只能刷新页面）；
     并按用户反馈加了**位移阈值**：拖拽（平移/旋转）松手后浏览器同样会补发 click，
     `pointerdown` 记录按下坐标，位移 >5px 视为拖拽、不退出——否则想拖着看当前高亮的子图，一松手就退出了。
- **验证（无头 Edge 实测，ma 微观层）**：
  | 状态 | DOM | 可见 | 隐藏 | 高亮 |
  |---|---|---|---|---|
  | 落地 | 86 | 86 | 0 | 0 |
  | 点击"1.1 定义" | 89 | **13** | **76** | 13 |
  | 点空白恢复 | 85 | **85** | 0 | 0 |

### 18. 对接前端负责人最新版（2026-09-15 下午）——覆盖后回填我们的集成改动
- **背景**：前端负责人交付最新版（`前端/SUFE-Knowledge-Galaxy最新版/`）：科研论文改为"三大顶刊三角分割 + 期刊文献列表 + PDF 在线阅读"、
  知识星系顶部改为 5 个图谱 tab、节点弹窗加 × 按钮等。已用其 `index.html` / `css/style.css` / `js/main.js` **整体覆盖**我们的同名文件；
  `js/galaxy.js` / `js/assistant.js` 未被覆盖（保留 graph_id 集成 + 9-15 全部修复）。
- **先备份**：动手前把当时的可演示版整体打包为根目录 `交付备份_前端_20260915.zip`（22.9MB）。
  ⚠️ 它实际是 **gzip/tar** 格式（magic `1f8b`，用 `tar -xzf` 解，`unzip` 会报错），内含 index.html/js/css/data/fonts/封面/backend 副本，不含视频 mp4。
- **对方基线是 9-14 交付版**，我们 9-14 晚以后的改动在新文件里全部缺失，已逐项回填：
  - `index.html`：书详情小图提示文案 → "本书知识图谱 · 宏观层（章节点）"。
  - `css/style.css`：① 书详情小图深色主题（.book-graph 背景 #141a26 + 红晕 + 红边，见 #8）；
    ② 配套 .bg-link / .bg-label / .book-graph-hint / .book-graph-loading 改浅色（#8）；
    ③ 宏观标签 30px、中观标签 20px 的字号补偿（#15）；④ `.g-label.low-q` 默认隐藏、hover 显示（#15）；
    ⑤ 两处 `:not(.faded)`（#17，否则"只看邻域"会被 has-focus 规则重新显示成 32%）。
  - `js/main.js`：① BOOKS 前 5 本元数据（投资学/公司金融/国际投资学/并购与重组/金融理论视频课）；
    ② `BOOK_GRAPH_API` 改绝对地址 `http://localhost:8000/api/graph/book` 并**接真实接口**（失败降级示例数据）；
    ③ 后端字段映射（`label`→`name`、`type`→`role`/半径，契约值域仍是 concept/chapter/section/formula）；
    ④ 七色配色 `NODE_COLORS`/`nodeColorOf` 回填并作用于 circle fill（#7）；
    ⑤ 把 `TYPE_TO_CLS`/`bgClassOf` 从 render 函数内部**提到模块级**——回填后 fetchBookGraph 的字段映射要用到它，
       否则首次打开书详情会 `ReferenceError: bgClassOf is not defined` 并静默降级成示例图（无头实测抓到并已修）。
- **新版 UI 适配**：新版用"知识星系"顶部 5 个 `.galaxy-tab[data-graph-id]` 取代我们右上角下拉框，
  `galaxy.js` 增加 tab 点击绑定（切换时同步 active 类、调用 switchBook；原 `#galaxyBookSelect` 监听保留兼容）。
- **结构性清理**（交付包里带回来的重复副本，均已删除）：`前端/SUFE-Knowledge-Galaxy/backend/`（controller 副本）、
  `assets/books/微信图片_*.png`（33 张，与我们的 01-35.png 逐字节相同）、根目录 `并购与重组_知识图谱.json`（与 `数据/` 同名文件相同）、
  `经济综合_知识图谱.json`（与 `数据/经济综合_知识图谱(旧版20260913).json` 相同）。**保留** `assets/papers/`（论文区要用）与 `assets/fonts/`。
- **验证（无头 Edge + CDP，真实后端 :8000 + 静态 :3000）**：

  | 检查 | 结果 |
  |---|---|
  | 点 5 个 tab 切书 | econ 2452 / corp_fin 1093 / intl_inv 913 / invest 331 / ma 138 节点；active 与 graphId 同步 |
  | 书详情小图 | `GET /api/graph/book/1` → 200；11 个章节点，标签"第1章 投资环境…"，7 种颜色 |
  | 层级切换（ma，光标锚点缩放） | 宏观 8/8、中观 52/52（可见标签 52）、`noTransform=0` |
  | 论文区 | 三角 3 扇区；点 JF → 期刊文献列表（Fama-French 等）正常 |
  | 学习资料 | 45 条视频清单加载成功 |
  | console | 无异常、无 BookGraph 降级警告 |
- **已知问题（非本次对接引入，建议转相关方）**：
  - 论文 PDF 只有 1 篇真实文件（`assets/papers/Agency cost of free cash flow…pdf`，对应 jfe 第 1 篇），
    JOURNALS 里另外 11 个路径（jf-01~04、rfs-01~04、jfe-02~04）均不存在，点击在线阅读会 404；
  - 视频封面 45×4 次 404 探测（probeCover 依次试 .jpg/.png/.jpeg/.webp，实际只有 01-cover.png），补封面即可消除；
  - `GET /api/graph/book/{id}` 目前只返回 11 个章节点 + 1 条 macro 边（书→章的 hier 边没返回），小图连线偏少，建议后端补 hier 边。

### 19. 论文区接入真实论文（13 篇 PDF）（2026-09-15 晚）
- **需求**：用户已把三刊论文放在 `数据/论文/{jf,jfe,rfs}/`，要求加载进前端。
- **文件**：13 个 PDF 复制到 `前端/SUFE-Knowledge-Galaxy/assets/papers/`，按约定重命名为
  `jf-01..04.pdf` / `jfe-01..04.pdf` / `rfs-01..05.pdf`（全 ASCII，避免中文与空格在 URL/iframe 中的转义问题；
  原先那篇长文件名 "Agency cost of free cash flow…" 一并规范化）。合计 17MB。
- **元数据按 PDF 正文实际内容重写**（原 JOURNALS 里是 Fama-French、Jegadeesh-Titman 等示例经典论文，与 PDF 内容完全不符）：
  用 pypdf 提取每篇首页文本取标题/作者/年份。⚠️ 发现两个**文件名与内容不符**（数据方或组长的命名问题）：
  - `jf/INTEREST RATES AND RETURN ON EQUITY OF DEPOSITMONEYBANKS IN NIGERIA.pdf` 内容实为
    "From Prediction to Decision: AI-Augmented Risk Systems and Capital Allocation in Regulated Financial Institutions"（Smitha Shivakumar）；
  - `jf/article_69f07a4db3c0a.pdf` 才是那篇尼日利亚利率论文（Ekujereonye 等，2026）。
  已**按内容归档**（原始文件未改动），两个文件名建议后续在 `数据/论文/` 里也改过来。
- **前端改动（js/main.js）**：
  1. `JOURNALS` 三刊条目全部替换为真实论文（JF 4 / JFE 4 / RFS 5；`cited` 暂填 0，列表显示"—"，排序稳定）；
  2. **新增可选字段 `source`**（论文实际出处），`renderJournal` 的出处行改为 `p.source || j.nameEn`
     —— 13 篇里多数是 NBER 工作论文/其他期刊（如 IJISS、JOFIGSS），直接显示所得顶刊名会误导；未填 `source` 时行为与原来一致；
  3. 更新字段说明与 PDF 命名约定注释（rfs 共 5 篇）。
- **验证（无头 Edge + CDP）**：三刊列表分别 4/4/5 篇，标题与出处正确；所有 PDF 按钮可点（`disabled=0`，
  即 `checkPdfExists` 全通过）；点首篇 → `#pdfMask` 打开、`#pdfFrame` 指向对应 `assets/papers/*.pdf`、未出现"原文 PDF 待补充"兜底；
  13 个路径对静态服务器 HEAD 全部 200；无 console 异常。
- 备注：PDF 已进版本库（17MB）。若希望像视频一样只留本地，可把 `前端/SUFE-Knowledge-Galaxy/assets/papers/` 加进 .gitignore（与 `数据/论文/` 同源，可随时再拷）。

### 20. 书详情小图改回白底 + 深色文字（2026-09-15 晚）
- **需求（用户反馈）**："点开一本书，显示知识图谱的部分怎么是黑色的，都看不清关系线了，把背景改成白色，字体改成黑色"。
- **文件**：`css/style.css`（仅样式，无 JS 改动）。原先 #8 的深色主题（背景 `#141a26` + 红晕）在 9-15 对接新版前端时被作为"回填项"恢复，
  本次按用户要求整体改回浅色：
  1. `.book-graph`：`radial-gradient(… rgba(129,28,33,.05) …) ×2 + #fff`，边框 `rgba(129,28,33,.12)`；
  2. `.bg-link` 描边 `rgba(129,28,33,.28)`、`.bg-link.weak` `rgba(129,28,33,.13)`（原深色底用的浅红改为适合白底的深红）；
  3. `.bg-label` fill `#222`、`.bg-label.center` fill `#111`（文字改黑）；
  4. `.book-graph-hint` 颜色 `rgba(129,28,33,.38)`（原 `rgba(201,209,217,.55)` 是深色底专用浅灰）；
  5. `.book-graph-loading` 遮罩 `rgba(241,240,233,.75)` + 字色 `var(--main-red)`（原 `rgba(20,26,38,.8)` + `#e5c9cb`）。
  > 注：第 18 条"回填项 ① 书详情小图深色主题"自本条起作废，以浅色为准（#8 同样失效）。
- **说明**：节点 circle 的填充由 `main.js` 的七色 `nodeColorOf()` 内联决定（白底上依旧可辨识），本次未动。
- **验证（无头 Edge + CDP，真实后端 + 静态服务器，打开 1 号书）**：
  `.book-graph` computed `background-color = rgb(255,255,255)`、border `rgba(129,28,33,.12)`；
  `.bg-label` fill `rgb(34,34,34)`；`.bg-link` stroke `rgba(129,28,33,.28)`；hint / loading 均为浅色；
  整个书详情弹层深色背景扫描仅剩 `detail-cover-box`（书籍封面占位块的主红底色，属原有设计）；11 节点 / 11 标签渲染正常，无 console 异常。

### 21. 论文 PDF 关闭"回到上次阅读位置"（2026-09-15 晚）
- **需求（用户反馈）**："现在打开一个论文会显示回到上一次看的位置，关掉这个功能"。
- **原因**：`openPdf` 每次给 iframe 设的都是**同一个 URL**，浏览器内置 PDF 阅读器会按 URL 记住上次的滚动位置并自动恢复。
- **文件**：`js/main.js`（openPdf）。改为每次打开都用一个新地址：
  `viewUrl = url + '?_t=' + Date.now() + '#page=1'`（已有 query 时用 `&`）——时间戳强制当作新文档重新加载，
  `#page=1` 再显式定位首页；`#pdfOpenNew` 的 href 仍用原地址（新窗口打开不受影响）。
- **验证（无头 Edge + CDP）**：连续打开同一篇两次，iframe src 分别为
  `assets/papers/jf-01.pdf?_t=1789468157081#page=1` / `…?_t=1789468160509#page=1`（两次不同、均带 `#page=1`）；
  静态服务器对带 query 的 PDF 返回 200 `application/pdf`（与不带 query 体积一致）；无 console 异常。

### 22. 图谱下方时间轴先注释掉（2026-09-15 晚）
- **需求（用户）**："图谱下面有一个时间轴显示，先不要这个了，把他注释掉，但不删代码"。
- **文件**：`index.html`（唯一改动，只有 1 处）。把 `#galaxyTimelineWrap` 整块（时间轴标签 + range 滑块 + 年份 + 复位按钮）
  用一段 HTML 注释包起来，注释里写明恢复方法；**CSS 与 galaxy.js 一行未动**。
- **为什么不影响功能**：`galaxy.js` 的 `setupTimeline()` 开头就是 `if (!wrap || !slider || !yearEl) return;`，
  元素取不到会直接跳过 → `state.timelineEnabled` 保持 `false`，`render()` 里的年份过滤分支（`time-faded`）与
  `resetView()` 里的滑块复位（`if (slider && …)`）都是空安全的。恢复：删掉外层注释即可，无需改 JS。
- **验证（无头 Edge + CDP）**：`#galaxyTimelineWrap` / `#galaxyTimeline` 均为 null；主页图谱正常渲染
  （宏观 46 节点 / 46 标签、`time-faded` = 0）；再切中观层 46 节点、`noTransform=0`；无 console 异常。

## 待前端负责人确认/合并的事项
- galaxy.js 的 graph_id 集成（switchBook、图谱 tab 监听、load/click/query 带 graph_id）请勿覆盖
- categoryOf 的 type→课程/概念映射是否要调整（影响环形布局与宏观层配色）
- 书详情小图的后端接口：GET /api/graph/book/{bookId}（1-4 对应 invest/corp_fin/intl_inv/ma，其余返回空图）
- 书籍封面 assets/books/ 仍有 33 张未按 01-35.png 重命名（与书籍对应关系需要人工核对）
- 第 18 条：新版为我们基线（9-14 交付版）之后的再次覆盖，回填项见该条，合并时请以项目内文件为准
- 第 19 条：JOURNALS 新增可选字段 `source`（论文实际出处），`renderJournal` 出处行改为 `p.source || j.nameEn`
- 第 20 条：书详情小图按用户要求整体改回**白底 + 深色文字**（第 18 条回填项 ①/第 8 条深色主题作废）
- 视频封面缺失（见第 18 条"已知问题"；论文 PDF 已补齐 13 篇）
