# 视图层对接流程（Mock 联调版）

> 给视图层同学：在真实的 data_service / qa_agent / main.py 就绪前，
> 先用 Mock 服务联调。接口路径、请求/响应结构不会变，之后后端切换真实实现，前端无需改动。

## 1. 环境准备（先安装依赖）

需要 Python 3.10+，执行：

```bash
pip install fastapi "pydantic>=2" uvicorn
```

安装完成后验证：

```bash
python -c "import fastapi, pydantic, uvicorn; print('ok')"
```

输出 `ok` 即可进入下一步。

## 2. 启动 Mock 服务

把收到的 `backend/` 目录放到本地后，在 **`backend/` 目录内**执行（必须在该目录下，否则导入路径会报错）：

```bash
cd backend
uvicorn mock_main:app --reload --port 8000
```

看到 `Uvicorn running on http://127.0.0.1:8000` 即启动成功。

- 接口文档（Swagger，可直接在线调接口）：http://localhost:8000/docs
- 健康检查：http://localhost:8000/health
- 已开启 CORS（允许所有来源），前端 dev server 可直接跨域调用

## 3. 接口总览

| 方法 | 路径 | 说明 | 请求体 |
|------|------|------|--------|
| POST | `/api/graph/load` | 页面初始化，加载全图 + 后台预热 Agent | `{ "session_id": "可空", "graph_id": "智能问答必填" }` |
| POST | `/api/graph/click` | 点击节点，返回一跳子图 | `{ "node_id": "必填", "session_id": "可空", "graph_id": "可空" }` |
| POST | `/api/graph/query` | 文本查询（关键词/自然语言自动分流） | `{ "text": "必填", "session_id": "可空", "graph_id": "智能问答必填" }` |

## 4. 统一响应契约（所有接口同构）

```json
{
  "code": 0,                 // 0 成功，500 失败
  "message": "success",      // 失败时是错误原因
  "session_id": "c5e084...", // 会话 ID，后续请求带回
  "data": {                  // 图谱子集
    "nodes": [{ "id": "n1", "name": "并购协同效应", "category": "概念",
                "media": { "text": "...", "images": [], "videos": [] } }],
    "edges": [{ "source": "n3", "target": "n1", "relation": "包含" }]
  },
  "answer": {                // 智能问答答案（自然语言查询时返回，其余接口为 null）
    "prediction_llm": "纯文本答案，无上标，用于复制/存档",
    "prediction_html": "带 <sup><a class=kg-node-link ...> 超链接的 HTML 答案，直接渲染",
    "related_nodes": [{ "id": "n1", "name": "并购协同效应", "type": "concept", "page": null }],
    "retrieval_count": 5,    // 检索命中候选数
    "used_count": 3          // 实际引用（标上标）的节点数
  },
  "actions": [],             // 动画指令序列，按顺序执行
  "degraded": false,         // true 表示已降级（Agent 超时/熔断）
  "notice": null             // 降级时的提示语，需要展示给用户
}
```

### actions 类型（动画指令契约）

| type | 含义 | 常用 params |
|------|------|-------------|
| `focus` | 聚焦目标节点 | `duration` |
| `highlight` | 高亮目标节点 | `duration` |
| `fade_in` | 淡入（新进入视野的节点） | `duration` |
| `fade_out` | 淡出（离开视野的节点） | `duration` |
| `expand` | 展开 | — |
| `zoom` | 缩放 | `mode: "fit" / "in" / "out"`, `duration` |
| `text_popup` | 文本弹窗 | `title`, `text` |

## 5. 请求/响应示例（来自实测输出）

**页面加载 `POST /api/graph/load`**，body `{}`：
```json
{"code":0,"message":"success","session_id":"c5e08443b464471ebdff485a62462695",
 "data":{"nodes":[...7个节点...],"edges":[...7条边...]},
 "actions":[{"type":"fade_in","targets":["n1",...,"n7"],"params":{"duration":400}},
            {"type":"zoom","targets":[],"params":{"mode":"fit","duration":600}}],
 "degraded":false,"notice":null}
```

**点击节点 `POST /api/graph/click`**，body `{"node_id":"n3"}`：
```json
{"code":0,"message":"success","session_id":"e4560b42d14f4b668549fbdbb0a5a0bc",
 "data":{"nodes":["n1","n3","n2","n5"对应的节点],"edges":[一跳内的边]},
 "actions":[{"type":"focus","targets":["n3"],"params":{"duration":300}},
            {"type":"fade_in","targets":["n1","n2","n3","n5"],"params":{"duration":400}}],
 "degraded":false,"notice":null}
```

**关键词查询（快通道）** body `{"text":"杠杆收购"}`：
```json
{"code":0,"message":"success","session_id":"cfbf561a...",
 "data":{"nodes":["n2"],"edges":[与n2相连的边]},
 "actions":[{"type":"highlight","targets":["n2"],"params":{"duration":500}},
            {"type":"zoom","targets":[],"params":{"mode":"fit","duration":600}}],
 "degraded":false,"notice":null}
```

**自然语言查询（慢通道，走 Agent）** body `{"text":"解释一下并购协同效应","graph_id":"gsjr"}`：
```json
{"code":0,"message":"success","session_id":"58f3aecc...",
 "data":{"nodes":["n1","n3","n2"],"edges":[...]},
 "answer":{"prediction_llm":"并购协同效应：并购后企业整体价值大于各部分之和...",
           "prediction_html":"根据知识图谱，并购协同效应<sup><a href=\"/knowledge/n1\" data-node-id=\"n1\" class=\"kg-node-link\">1</a></sup>...",
           "related_nodes":[{"id":"n1","name":"并购协同效应","type":"concept","page":null}],
           "retrieval_count":1,"used_count":1},
 "actions":[{"type":"focus","targets":["n1"],"params":{"duration":300}},
            {"type":"highlight","targets":["n1"],"params":{"duration":500}},
            {"type":"zoom","targets":[],"params":{"mode":"fit","duration":600}}],
 "degraded":false,"notice":null}
```

**上标超链接跳转**：前端渲染 `answer.prediction_html` 时，给所有 `.kg-node-link` 绑定点击事件：
点击后读取 `data-node-id`，调 `POST /api/graph/click`（body 带该 node_id + graph_id），
控制层会返回 `focus + highlight` 动作，图谱画布定位并高亮该节点。

## 6. 联调必测场景清单

按顺序过一遍，每步都验证 `actions` 动画效果：

1. **加载**：页面打开调 load → 全图淡入 + 缩放适配
2. **点击**：点一个节点调 click → 聚焦 + 新节点淡入；再点相邻节点 → 旧节点 fade_out（离开视野时）
3. **关键词查询**：输入短词如"杠杆收购" → 高亮 + 缩放（响应很快，毫秒级）
4. **自然语言查询**：输入含疑问句的长句如"解释一下并购协同效应" → 返回带 `<sup>` 上标超链接的答案（渲染 `prediction_html`）+ 图谱定位高亮引用节点（有约 0.5s 延迟，是 mock 模拟的慢通道）
5. **上标跳转**：点击答案里的上标 `[1]` → 前端调 `/api/graph/click` → 图谱聚焦 + 高亮该节点
6. **降级展示**：响应中 `degraded=true` 时，前端要显示 `notice`（"智能分析暂时不可用，已切换到基础检索模式"）

## 7. 注意事项

- **session_id 流转**：第一次请求（load）返回的 `session_id`，后续 click/query 请求必须原样带回，否则后端视为新会话（影响 fade_in/fade_out 的视野计算）。无 session_id 也能用，只是每次都是新会话。
- **graph_id**：用户停留在哪本书就传哪本书的图谱标识（统一编码表，四方共享）：

  | graph_id | 图谱 |
  |----------|------|
  | `ma` | 并购与重组 |
  | `corp_fin` | 公司金融 |
  | `intl_inv` | 国际投资学 |
  | `econ` | 经济综合 |

  load/query 不带 graph_id 时智能问答自动降级为基础检索（`degraded=true` + 对应 notice）。
- **code != 0 处理**：失败时 `data` 为 null，前端要读 `message` 展示错误，不要崩溃。
- **actions 按数组顺序执行**，targets 里的 id 对应 `data.nodes[].id`。
- **prediction_html 直接渲染前需防注入**：该 HTML 由 Agent（LLM）生成，前端渲染时只允许 `<sup>`/`<a>` 等白名单标签（或转义后重建链接），不要用 innerHTML 直接塞任意内容。
- **Mock 数据仅 7 个节点**（两门课 + 概念节点），用于渲染调试；真实图谱会大很多，前端渲染需考虑性能。
- **Swagger 测试中文乱码**：在 `/docs` 页面里调接口没有编码问题；命令行 curl 发中文需确保 UTF-8。
- 真实实现就绪后，后端把 `mock_main.py` 换成正式 `main.py`，接口不变。
