# 整合改动记录（2026-09-14，已整体回退）

> 本文件记录 2026-09-14 当天"数据接入 + 前端整合"的全部改动。
> 当天 5 个提交（5f77904 → 8be8e85）已按用户要求用 `git revert` 全部撤销，
> 代码回到 cae03e4（"刚拿到数据文件时"的状态）。本文件为唯一留存记录，
> 后续重新整合时可参照此文档与 git 历史（revert 提交本身也在历史中）。

## 一、当天完成的改动（均已被 revert）

### 1. 数据层数据接入（5f77904）
- 5 个新知识图谱 JSON（4 本书 + 经济综合融合图）从 `知识图谱和书和视频/` 复制到 `data/`
- 新增图谱编码 `invest = 投资学`（graph_ids.py + data_service.py GRAPH_FILES）
- 前端"当前图谱"下拉框加《投资学》选项（共 5 项）
- 新增离线 embedding 缓存生成脚本 `backend/generate_cache.py`（`--force` 全量重建）
  - 用新数据为 5 书生成 embedding 缓存到 backend/cache/（补上了 ma，新增 invest）
- 移除根目录旧版 4 个知识图谱 JSON（被 data/ 新版替代）
- `.gitignore` 增加 `知识图谱和书和视频/`（大书 JSON 约 2GB + 视频不进仓库）
- main.py 预热注释 4 本书 → 5 个图谱

### 2. 课程书架填充（1cbf829）
- `前端/SUFE-Knowledge-Galaxy/js/main.js` 的 BOOKS 数组前 5 个槽位填真实元数据：
  投资学（第十版）/ 公司金融（进阶篇）/ 国际投资学（第二版）/ 并购与重组：中国案例 / 金融理论（视频课程）
- 其余 30 本仍为空（待书单）

### 3. 书籍详情接入知识星系（fbe783a）
- 书籍详情弹窗"知识图谱"区域改为动态：有图谱的书显示"在星系中查看"按钮，
  点击跳转知识星系并调用 `GalaxyEngine.switchBook(graph_id)`
- BOOKS 前 4 项加 `graphId` 字段（invest / corp_fin / intl_inv / ma）

### 4. 修复节点文字不显示（c704651）
- galaxy.js 三处（节点文字/悬停标题/弹窗标题）`d.name` → `d.name || d.label`
  （后端字段是 label，前端旧代码读 name）
- style.css 删除中观层隐藏节点文字的规则 `.g-node.zoom-meso.other .g-label{opacity:0}`

### 5. 图谱节点七色配色（8be8e85）
- galaxy.js 新增 7 色调色板（红橙黄绿青蓝紫），按节点 id 哈希稳定分配颜色，
  内联 style 覆盖原灰色样式

## 二、回退记录

| Revert 提交 | 撤销对象 |
|---|---|
| cadfa21 | 8be8e85 七色配色 |
| aee1bcc | c704651 label 修复 |
| 6e0174a | fbe783a 书籍详情跳转 |
| 0db2016 | 1cbf829 书架填充 |
| 3ffb051 | 5f77904 数据接入 |

回退后代码 = cae03e4 状态：`data/` 目录已删、根目录旧版 4 个 JSON 已恢复、
invest 编码已移除、前端下拉框回到 4 项。

## 三、回退后本地状态（未受 revert 影响的部分）

- `知识图谱和书和视频/`：数据文件原样保留（5 个知识图谱 JSON + 4 个约 500MB 大书 JSON + 23 个视频）
- `backend/cache/`：5 本书的 embedding 缓存仍在（gitignored，重新接入数据后可直接复用）
- `backend/config.json`：API key 本地配置仍在（gitignored）

## 四、遗留问题清单（重新整合时需处理）

### 阻断性
1. 节点文字不显示：后端发 `label`，前端读 `d.name`（galaxy.js 渲染/悬停/弹窗三处）+ 中观层 CSS 隐藏文字
2. 图谱一直动 + 卡顿：力导向布局慢衰减（经济综合 5437 节点/5315 边），
   每帧更新 1.6 万+ SVG 元素，中观层渲染全部 5315 个关系文字标签

### 契约未对齐
3. 前端按 `node.category`（课程/概念）分环分样式，后端只发 `type`（concept/chapter/section/formula）
4. `media` 语义：字段.md 定为 {image_url, video_url}，前端却读 media.text（节点说明）/ media.year（年份）
5. 时间轴用 id 哈希伪造 2010-2024 年份，数据无真实年份（数据层可加 extra.year）
6. prediction_html 前端未用（助手用 related_nodes 自建链接），契约保留

### 数据质量（数据层）
7. 投资学图谱混入 OCR 垃圾节点（"Graw"、"Education"、"INVESTMENTS" 等封面文字）
8. 公司金融标题多括号："（（英）理查德A.布雷利）"
9. 经济综合 title = "经济学综合知识图谱"

### 前端待建设
10. 星系分支 tab"拓展星系一/二"仍为"待建设"占位
11. 课程书架 30 本书单缺失；封面图未按 01.png~35.png 重命名（第 3~35 本无封面）
12. 视频播放区域（等最新版前端 + HTTP 协议）

### 部署小项
13. 前端 API_BASE 硬编码 localhost:8000
14. backend/config.json 明文 API key 待改环境变量（AGNES_API_KEY / AGNES_BASE_URL）

### 已对齐（无需再动）
- graph_id 编码、统一响应 {code,message,session_id,data,answer,actions,degraded,notice}
- /load /click /query 请求字段（session_id/graph_id/text/node_id）
- actions 六种类型兼容、助手 related_nodes {id,name} 可点击联动
- 前端无 mock 残留
