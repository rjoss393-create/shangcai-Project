# 新 AI 快速上手（一页版）· 2026-09-20

> 详细内容看同目录 `AI交接文档_2026-09-20.md`。本页只回答"先读什么、别碰什么、先干什么"。

## 一、先读这 4 份（按顺序）

1. `文档/交接/AI交接文档_2026-09-20.md` —— 第 1、2、6 节必读（身份 / 架构契约 / 用户规矩）。
2. `文档/协议/controller.txt` —— 控制层权威设计（注意：根目录那份 `controller.txt` 是**旧版**，冲突时以此归档版为准，❓ 但需向用户确认）。
3. `文档/记录/前端改动记录.md` 第 27~32 条 —— 最近两天（9-18 / 9-19）到底改了什么。
4. `文档/设计/登录与用户管理设计.md` —— 认证与个人状态接口契约。

## 二、别碰什么

- ❌ `backend/agent/`、`backend/src/`、`backend/dataset_files/`、`run_rag.py` —— Agent 层同学的。
- ❌ `前端/**` —— 前端负责人的。要前端配合时：先找**后端同路径提供数据**的等价方案，或列清单交对方，**先问用户**。
- ❌ `数据/` 目录 —— gitignore 的原始交付（4.8GB），**不要提议上传 GitHub**。
- ❌ 不要 amend / force push；不要替用户提交未确认的工作区改动。

## 三、开工前 3 条命令

```bash
cd E:/上财项目(控制层)
python -m uvicorn main:app --host 127.0.0.1 --port 8000   # 页面 http://localhost:8000/index.html
curl -s http://127.0.0.1:8000/health                       # 期望 {"status":"ok","agent":true}
cd backend && python -m pytest -q                          # ✅ 期望 118 passed
```
正常标志：启动日志 5 行 `[preload] xxx 预热完成`；`agent` 必须是 `true`（false = API Key / 向量模型缓存没配好）。

## 四、现在最该做的事（按顺序，P0 先做）

1. **问用户**：9-18/9-19 的未提交改动是否自测通过（代码是用户要求"先自测、暂不提交"才留在工作区的）。
2. 通过后**拆成独立 commit**（媒体迁移 / 登录+个人状态后端 / 前端新版合并+深色主题 / 记录文档），再 `git -c http.version=HTTP/1.1 push origin main`（本地已有 5 个提交未推）。
3. 小清理（后端侧）：`data/profiles.json` 加进 `.gitignore`；根目录旧副本 `前端改动记录.md`、未跟踪的前端交付包 `SUFE-Knowledge-Galaxy/` 按用户意见清理或 ignore。
4. 待确认的大项：根目录前端新包（**课程星系** + `galaxy.js`/`style.css` 重写，9-19 晚）尚未合并 —— 问用户是否合并、由谁合并；后端可能的配合点是 `/data/courses.json` 与学习进度持久化。

## 五、最容易踩的坑

| 坑 | 正确做法 |
|---|---|
| 直连 github push 失败 | `git -c http.version=HTTP/1.1 push origin main` |
| 以为前端数据还在前端 | 数据在 `data/media/`，后端按前端原路径提供（`backend/controller/media.py`） |
| 改完数据不清缓存 | 清 `backend/cache/<gid>_all-MiniLM-L6-v2/` 再重启后端 |
| 用无头浏览器验证却跑在 8010 | 必须跑 :8000（`galaxy.js` 的 `API_BASE` 写死 localhost:8000） |
| 想"看图/看截图"判断问题 | 本 AI 无图像能力；用 `E:/galaxy_probe/_tools/ocr_covers2.ps1`（Windows OCR）拿文字 |
| 以为提交历史都在远端 | 远端停在 `83f38e0`，本地另有 5 个提交（`00cde26`…`5fef341`）未推 |
