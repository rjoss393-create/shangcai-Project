# 前端改动说明与对接约定（2026-09-21）

## 本次内容：① v6 统一知识星系接入 + ② Chart.js 本地化（修复「指数不显示」）

> 这份文档既是**本次改动记录**，也是**前端以后更新时要遵守的对接约定**（见第六节）。
> 改动前的方案稿见 `文档/交接/前端接入v6图谱_2026-09-21.md`（已被本文取代，留作评审记录）。
>
> **本次共动了 5 个前端文件**：A 组 3 个属 v6 接入（第一节），B 组 2 个属 Chart.js 本地化（第五节）。
> 两组互相独立，可分别回滚（见第八节）。
>
> 📌 **2026-09-22 更新**：你后来交付的 **v12 引擎 + page-slider** 已并入线上
> （合并记录见 `文档/记录/前端改动记录.md` 末尾「2026-09-22」条目；v12 已在后端注册为
> `graph_id='v12'`，页面用 `window.__KG_DATA_SOURCE__='api'` 走后端取数）。
> 你照着本文第六节写的引擎我们**零改动合入**——这证明约定的写法是可执行的，以后请继续照此更新。
> 交付包目录已从仓库根目录移除（原始文件备份在我方机器上，需要时可取回）。

---

## 一、这次改了 5 个文件

### A 组：v6 统一知识星系接入（3 个文件）

| 文件 | 动作 | 说明 |
|---|---|---|
| `js/knowledge-galaxy.js` | **新增**（69 KB，来自新包） | v6 统一知识星系引擎 |
| `css/knowledge-galaxy.css` | **新增**（21 KB，来自新包） | 引擎自带样式 |
| `index.html` | **改 3 处**（788 → 831 行） | 见下 |

`index.html` 的三处改动：

1. `<head>` 里在 `css/course-galaxy.css` 之后加一行：
   ```html
   <link rel="stylesheet" href="css/knowledge-galaxy.css">
   ```
2. **只替换 `<section id="galaxy">` 的内部内容**（第 262~340 行 → 新包的 121 行）：
   旧的 `galaxy-tabs`（5 个图谱页签）、`galaxy-zoom-indicator`、`galaxy-nebula/vignette`、
   `galaxyNodePopup`、`galaxyImmersiveExit`、`galaxy-hint` 等标记，换成新引擎的
   `kg-root / kg-topbar / kg-sidebar / kg-stage / kg-toolbar / kg-legend / kg-detail`。
   `<section>` 开标签本身（`id`/`data-require-login`/`data-guard-title`）**原样不动**。
3. 脚本引用 `js/galaxy.js` → `js/knowledge-galaxy.js`（只有这一处）。

**A 组没动的部分**（重要）：`js/main.js`、`js/auth.js`、`js/assistant.js`、`js/course-galaxy.js`、
`js/profile-ui.js`、`css/style.css`、市场行情板块、新闻板块、导航栏（含 🌓 昼夜主题按钮）、
课程星系板块、书架、论文板块 —— **一律保持我方集成版本**。

> 新包（`newcssSUFE-Knowledge-Galaxy/`）里另有市场/新闻板块重构、导航栏去掉主题按钮、
> `assistant.js` 退回 mock、`auth.js` 打开假登录等改动，**全部没有采用**。
> 原因见 `文档/记录/整合改动记录_2026-09-14.md` 与我方 `MEMORY` 里的教训：
> 新包是"前端负责人在我们整合之前的快照"上做的，一半新一半旧。

### B 组：Chart.js 本地化（2 个文件）

| 文件 | 动作 | 说明 |
|---|---|---|
| `js/chart.min.js` | **新增**（205 KB） | Chart.js **v4.4.1** UMD 构建，供本地引用 |
| `index.html` | **改 1 处**（第 17~18 行） | 图表库引用：CDN → 本地 `js/chart.min.js` |

**动机**：用户上线实测报障「**指数怎么不显示了**」，根因就是这个 CDN 引用。
完整原因、改法（可直接照抄）与验证数据见**第五节**。B 组与 A 组完全独立，可以单独回滚。

---

## 二、为什么新引擎要接后端（这次的核心）

页面上原本有两条互不相干的链路：

| | 改动前 | 改动后 |
|---|---|---|
| **画布显示** | `fetch('data/galaxy_v6_clustered.json')` 读本地文件，节点 `kp_*` | `POST /api/graph/load {graph_id:'v6'}`，同一批 `kp_*` |
| **助手问答** | `assistant.js` 读 `state.graphId` → 取不到，兜底 `'econ'` → 后端返 `concept_*` | 同一个 `graph_id:'v6'` |

改动前：答案里引用的 `concept_*` 节点在 v6 画布上不存在 → 高亮全部失效，答案讲的也是另一张图。
改动后：两侧同源，`kp_*` 能直接命中画布节点。

---

## 三、`js/knowledge-galaxy.js` 的 4 处改动（对照后端契约）

### 1) 数据源改走后端（原第 25 行常量 + `load()` 函数）

```js
/* 原 */
const DATA_URL = 'data/galaxy_v6_clustered.json';
...
loadPromise = fetch(DATA_URL)
    .then(r => { ... return r.json(); })
    .then(data => { state.data = data; state.idx = buildIndex(data); ... });
```
```js
/* 现 */
const GRAPH_ID = 'v6';              /* 后端 controller/graph_ids.py 里登记的图谱 */
const API_BASE = '';               /* 与页面同源；页面不由后端托管时写 'http://localhost:8000' */
const DATA_URL = 'data/galaxy_v6_clustered.json';   /* 保留作参考，已不再使用 */
...
loadPromise = fetch(API_BASE + '/api/graph/load', {
        method:  'POST',
        headers: { 'Content-Type': 'application/json' },
        body:    JSON.stringify({ graph_id: GRAPH_ID })
    })
    .then(r => { if (!r.ok) throw new Error('HTTP ' + r.status); return r.json(); })
    .then(resp => {
        if (!resp || resp.code !== 0) throw new Error((resp && resp.message) || '接口返回异常');
        const d = resp.data || {};
        /* 后端契约 {id,label,page,layer,media,extra} -> v6 引擎原字段名 */
        const data = {
            nodes: (d.nodes || []).map(n =>
                Object.assign({}, n.extra || {}, { id: n.id, name: n.label })),
            edges: (d.edges || []).map(e =>
                Object.assign({}, e.extra || {}, {
                    source: e.source, target: e.target, relation: e.relation
                }))
        };
        state.data = data;
        state.idx = buildIndex(data);
        ...   /* 以下十余行一个字没改 */
```

> **为什么适配只要一行**：后端转换脚本把 v6 的原始字段名（`level` / `parent_id` / `content` /
> `cluster_id` / `cluster_name` / `domain` / `tags` / `difficulty` / `importance` / `is_external` …）
> **原样保留在 `extra` 里**，所以 `Object.assign({}, n.extra, {id, name})` 就能把节点还原成
> v6 引擎认识的形状。引擎完全不用改。

### 2) `state` 加 `graphId`（原第 62 行附近）

```js
        currentGraphId: 'econ',   /* 兼容旧引擎契约（课程星系移植时读写） */
        graphId: 'v6',            /* ★ 助手提问用；assistant.js 读 state.graphId */
```

> **不要**写成 `Object.defineProperty(state,'graphId',{get:()=>state.currentGraphId})`。
> 老引擎（`js/galaxy.js` 第 119~124 行）是那么写的，但新架构下画布**永远显示 v6**，
> 而 `course-galaxy.js`（第 551 行）跳转时会 `switchGraph('econ')` 把 `currentGraphId` 改成 `econ`
> —— 用别名会让助手一旦被课程星系"带偏"就再也问不到 v6。

### 3) 补 `state.nodes` 别名

```js
    /* 兼容旧引擎的 state.nodes：course-galaxy.js 用它按 id 找节点 */
    Object.defineProperty(state, 'nodes', {
        get() { return (state.data && state.data.nodes) || []; },
        enumerable: true, configurable: true
    });
```

### 4) 补 `GalaxyEngine.clickNode`（含层级路由）

```js
        clickNode(idOrName) {
            const idx = state.idx;
            const n = idx && idx.byId.get(idOrName);
            if (n) {
                if (n.level === 'macro') enterMacro(n.id);
                else if (n.level === 'meso') enterMeso(n.id);
                else if (n.level === 'micro') jumpToMicro(n.id);
                else if (n.level === 'explanation') jumpToMicro(n.parent_id || n.id);
                else goOverview();
                return;
            }
            this.selectNode(idOrName);
        },
```

新引擎只导出 `selectNode`（且只认 `micro` 层），而 `course-galaxy.js` 第 542 行传进来的是
`kp_macro_*`（学科节点），所以必须按层级分流，否则「课程星系点概念 → 跳知识星系」只会 toast
「未找到对应概念节点」。

---

## 四、自测结果（`E:/galaxy_probe/verify_v6_frontend.mjs`，无头 Edge + CDP）

```
graphId=v6            PASS
走后端 load           PASS   (请求体 {"graph_id":"v6"})
query 带 graph_id=v6  PASS   (请求体 {"text":"…","graph_id":"v6"})
总览层画出节点        PASS   (29 个)
节点不重叠            PASS   (最近两点间距 73px)
助手答案高亮命中      PASS   (点「查看图谱证据」→ kg-pulse 命中 kp_micro_0002)
无 JS 异常            PASS
```

过程数据：`state.nodes=3661 / edges=6058`；节点字段还原为
`{parents, parent_id, level, domain, id, name}`；
`clickNode('kp_macro_005')` → `view=macro`；`clickNode('kp_micro_0001')` → `view=meso, mesoId=kp_meso_061`；
助手答案里出现可点的 `kp_micro_0002 货币职能`、`kp_l4_0002 流通手段`。

全站冒烟（`E:/galaxy_probe/smoke_after_v6_frontend.mjs`）：8 个导航按钮全部可用、
课程星系 `#cgSvg` 渲染 103 个元素、书架 35 项、行情图表、🌓 主题按钮、助手悬浮球都在，**JS 异常 0 条**。
后端 `pytest`：**121 passed**（未受影响）。

---

## 五、补丁：Chart.js 从 CDN 改为本地引用（修复「指数不显示」）

### 1) 现象（用户上线实测报的）

> 「等一下，指数怎么不显示了」

打开财经板块：**实时指数表是空的、下面的走势图一片白**。

### 2) 根因（控制台原话）

用无头 Edge + CDP 抓到的异常：

```
LOG.error : Failed to load resource: net::ERR_CONNECTION_RESET
            @https://cdn.jsdelivr.net/npm/chart.js
EXC       : ReferenceError: Chart is not defined
              at initChart (js/main.js:255)
              at HTMLDocument.<anonymous> (js/main.js:649)
```

链条是这样断的：

1. `index.html` 第 18 行原本从 **jsdelivr CDN** 拉 Chart.js。国内网络访问 `cdn.jsdelivr.net`
   经常在 TLS 层被 `ERR_CONNECTION_RESET` → `window.Chart` 始终是 `undefined`。
2. `js/main.js:648` 的 `DOMContentLoaded` 回调里，**`initChart()` 是第一条语句**（第 649 行），
   它在第 255 行 `new Chart(...)` 抛 `ReferenceError` → **这个回调从这一行起就中断了**。
3. 于是同一回调里排在后面的 `setupStockTabs()` / `startRealtimeLoop()` / `loadNewsFromApi()`
   **全部没有执行** → 指数表 `#stock-data` 渲染 0 行、走势图停在 canvas 默认的 300×150 且什么都没画。

> ⚠️ 关键认知：受影响的**不是一个图表，而是整个「财经 / 新闻 / 职位」板块的初始化**。
> 排查这类"某块空白"的问题，**先看控制台异常**，别急着猜 CSS 或布局。

> 补充：这一行 CDN 引用在 v6 接入**之前就存在**（`git show de3e684~1:前端/SUFE-Knowledge-Galaxy/index.html`
> 第 17 行即可确认），属既有隐患，恰好这次上线测试暴露出来。

### 3) 改法（照抄即可）

**第 1 步**：把 Chart.js 的 UMD 构建放到 `js/chart.min.js`。

> ✅ **最省事的做法**：`js/chart.min.js`（205,086 字节）**已经在仓库里了，直接取用即可，不用自己下载**。
> 下面的命令只是记录来源，供以后升级版本时复现。注意**下载动作本身也走 jsdelivr**，
> 如果你的网络拉不动，就从仓库里拷贝这个文件。

```bash
cd 前端/SUFE-Knowledge-Galaxy
curl -L -o js/chart.min.js \
  https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js
```

**第 2 步**：改 `index.html` 第 17~18 行：

```html
<!-- 原 -->
<!-- 图表库 -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<!-- 现 -->
<!-- 图表库（本地化：原先走 jsdelivr CDN，国内网络常被重置导致 Chart 未定义、行情图表与指数表不渲染） -->
<script src="js/chart.min.js"></script>
```

> **两个细节**：
> 1. 下载后建议删掉 jsdelivr 自动插入在文件头的横幅注释、以及结尾的 `//# sourceMappingURL=...`
>    （否则浏览器会去请求不存在的 `.map`，控制台多一条 404 噪音）。
> 2. 文件里的 `/*! Chart.js v4.4.1 ... Released under the MIT License */` **许可头必须保留**。

### 4) 效果（无头 Edge + CDP 实测）

| 指标 | 修复前 | 修复后 |
|---|---|---|
| `typeof window.Chart` | `undefined` | `function` |
| `Chart.getChart(#stockChart)` | 无实例 | 有实例 |
| canvas 实际尺寸 | 300×150（默认值） | **1199×320** |
| `#stock-data` 行数 | **0** | **9**（首行 `上证指数 3940.55 上涨`） |
| canvas 绘制率（采样非透明像素） | 0 | **0.424** |
| 控制台异常 | `ReferenceError: Chart is not defined` | 仅 `favicon.ico` 404（原有，无害） |

同一个回调里被中断的初始化也一并恢复：
9 个指数页签、指数表 9 行、**国内新闻 20 条 + 国际新闻 3 条**、职位卡 10 个、书籍 35 本，**JS 异常 0 条**。

回归验证（确认没碰坏 v6）：`verify_v6_frontend.mjs` 仍 **7/7 PASS**、
`smoke_after_v6_frontend.mjs` 8 个板块全活、0 异常。

---

## 六、★ 对接约定（前端以后更新请照这个来）

1. **知识星系的数据必须来自后端**，`graph_id = 'v6'`，走 `POST /api/graph/load`。
   不要再让引擎 `fetch` 本地 JSON —— 否则前端和后端各存一份 v6 数据，谁更新了都会静默不同步。
2. **`state.graphId` 必须等于"画布上此刻真正显示的那张图"**，且**不能**做成 `currentGraphId` 的别名。
   谁改画布显示内容，谁负责同步 `graphId`。
3. **`window.GalaxyEngine` 的对外壳要保住向后兼容**：`clickNode` / `selectNode` / `pulseNodes` /
   `focusOnNodes` / `switchGraph` / `goMacro` / `state.nodes` / `state.graphId`。
   知识助手 `assistant.js` 和课程星系 `course-galaxy.js` 都靠这层壳联动；换引擎内部实现可以，壳别拆。
4. **节点字段契约**：后端给的是 `{id, label, type, page, layer, media, extra}`，
   层级信息在 `extra.parents`（数组）与 `extra.level` 里。
   前端适配写 `Object.assign({}, n.extra, { id: n.id, name: n.label })` 即可拿到
   `level` / `parent_id` / `content` / `cluster_id` 等全部原字段。
   若以后换成别的数据源，**`level` 和 `parent_id` 这两个字段名必须保留**（引擎的分层树靠它们）。
5. **替换某个板块的 HTML 前，先清点该板块的 DOM 被哪些 js 引用**。
   本次只换知识星系板块，是因为核对过：旧标记（`galaxyTabs`/`galaxyZoomLevel`/`galaxyNodePopup`/
   `galaxyStars`/`galaxyToast`/`galaxyImmersiveExit`/`galaxyTooltip`）**只被 `galaxy.js` 自己引用**，
   `main.js`、`assistant.js`、`course-galaxy.js` 都不依赖；唯一的外部引用是 `assistant.js` 用了
   `#galaxyStage`，而新标记里保留了同名 id。
6. **不要回退既有集成**：`main.js`（七色配色 / 昼夜主题 `sufe_theme` / PDF `#page=1`）、
   `auth.js`（真登录，`USE_MOCK:false`）、`assistant.js`（已对接 `/api/graph/query`）。
   交付新前端包时请以这几个文件的我方版本为准，或先说明改了哪里。
7. **数据结构变了要同步后端**：v6 图谱数据由 `scripts/import_v6_galaxy.py` 从
   `data/layered/v6_layered.json` 提供给后端；若前端要改节点/边的字段，先提出来，由后端侧同步转换脚本，
   不要单方面在前端"补字段"。换数据后需清 `backend/cache/v6_all-MiniLM-L6-v2/` 再重启后端。
8. **`graph_id` 的取值表是共享契约**：`ma / corp_fin / intl_inv / invest / econ / v6`，
   定义在 `backend/controller/graph_ids.py`。新增图谱只加行、不改旧值。
9. **★ 不要把任何第三方库改回 CDN 外链**（`cdn.jsdelivr.net` / `unpkg` 等）。
   国内网络访问 `cdn.jsdelivr.net` 经常被 `ERR_CONNECTION_RESET`；库一加载失败会
   **连带整块初始化一起挂掉**（2026-09-21 实测：Chart.js 拉不到 → 指数表空、走势图白）。
   **页面里所有第三方库统一放 `js/` 下本地引用**（先例：`js/d3.min.js`、`js/chart.min.js`）。
   整站本来就跑在本地后端 `:8000`，本地引用同时也让离线/内网环境可用。完整说明见**第五节**。

---

## 七、已知限制（不是 bug，暂不在本次修）

1. **Agent 答案引用 `kp_l4_*`「解释」节点时不会高亮**：引擎的 `pulseNodes` / `focusOnNodes` / `selectNode`
   只认 `level === 'micro'`（`knowledge-galaxy.js` 第 1546 行起），L4 名字带「-解释」后缀，
   名称兜底也匹配不上。共 1675 个 L4 节点。本次实测答案里 `kp_l4_0002 流通手段` 可点但无反应，
   `kp_micro_0002 货币职能` 正常。
2. **158 个 `kp_ext_*` 跨学科节点默认不显示**（`is_external` 过滤），切到「扩展视图」才可见。
3. **v6 没有页码**，节点详情里没有"教材第 N 页"。
4. **Agent 层两处对旧数据集的硬编码**（`backend/src/`，Agent 层同学的代码，未动）：
   v6 问答不做"同章兄弟节点"扩展、上下文里没有章级提示。
   见 `文档/记录/v6图谱接入后端记录.md` 第四节第 3 条。
5. **新包还有这些没合并**（要不要合由前端负责人定）：
   课程星系板块的「图谱切换」入口（新包把它从知识星系挪到了课程星系）、
   市场行情/新闻板块的一行式重构、`course-galaxy.js` 新版的 810 行实现。
   现状是我方课程星系 + 我方 `course-galaxy.js`（694 行），能正常跳转与聚焦。

---

## 八、回滚方法

**A 组（v6 接入）回滚：**

```bash
git checkout -- 前端/SUFE-Knowledge-Galaxy/index.html
rm 前端/SUFE-Knowledge-Galaxy/js/knowledge-galaxy.js
rm 前端/SUFE-Knowledge-Galaxy/css/knowledge-galaxy.css
```

（`index.html` 改动前的副本另备一份在 `E:/galaxy_probe/backup_index_2026-09-21.html`。
回滚后知识星系会回到旧的 `galaxy.js` 三档布局引擎，后端 `v6` 图谱仍在，只是页面上不再展示。）

**B 组（Chart.js 本地化）回滚：**

```bash
git checkout -- 前端/SUFE-Knowledge-Galaxy/index.html   # 把第 18 行改回 CDN 写法
rm 前端/SUFE-Knowledge-Galaxy/js/chart.min.js
```

> ⚠️ **不建议回滚 B 组**：回滚等于把「指数不显示」的故障再装回去。
> 若确实要用 CDN，请换成国内可达的镜像（如 `cdn.bootcdn.net`），并自行承担可用性风险。
> 注意 A、B 两组都动了 `index.html`，**单独回滚某一组时不要 `git checkout` 整个文件**，
> 只改对应那几行。
