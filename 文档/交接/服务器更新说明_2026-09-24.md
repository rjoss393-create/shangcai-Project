# 服务器更新说明（2026-09-24）

> 收件人：服务器部署负责人
> 上一版交付：`部署更新包_2026-09-23.zip`（对应版本库提交 `d9fc961` + 该包覆盖后的状态）
> 本次交付：`部署更新包_2026-09-24.zip`（**约 17.0 MiB ≈ 17.8 MB**，**不含教材**——教材上一版已交付）

---

## 0. 一句话概括

本次更新两件事：**课程星系换上新版课程知识图谱并接入「知识点小漫画」**；**顺带补上上一版没发出去的前端最终版**。

1. **补发前端最终版**（上一版之后才合并的）：新增**「数据库」板块**、行情侧栏支持三日/单日 + 成交量、课程图谱支持切换，
   框架与样式整体升级。上次的包是 09-23 22:33 打的，这次合并是 23:24 才提交的，**没有进上一版的包**，本次一并补上。
2. **课程星系换图**：投资学 / 公司金融 / 并购与重组三个图谱的数据源从「旧分层图谱」换成**新版 6 层课程图谱**
   （投资学 **28 章**（旧图只有 11 章），可一直钻到「名词解释 / 深层内容」层）。
3. **知识点小漫画接入**：用户提供的 24 部 / 144 页漫画，按「章」挂到知识点上。
   点知识点 → 右侧详情面板出现 **「🎨 看图解漫画」** → 全屏翻页看整部（6 页：封面 / 4 个知识点 / 小结）。

> **体积口径**（别混）：
>
> | 看什么 | 数字 | `du -h` 显示 |
> |---|---|---|
> | **压缩包** `部署更新包_2026-09-24.zip` | 约 17.8 MB（约 17.0 MiB） | **约 17M** |
> | **解压落盘** 全部 177 个文件 | 28,540,205 字节 = 28.5 MB = **27.22 MiB** | **约 27M** |
> | 其中 `data/media/comics/`（144 个 jpg） | **22,418,451 字节** = 22.4 MB = **21.38 MiB** | **22M** ← 第 3 节核对用这个 |
> | 其中 `data/media/graphs/course_graph_*.json`（3 个） | **4,308,642 字节** = 4.11 MiB | 4.2M |
> | 其余（前端 15 个 + 后端 7 个 + 脚本/文档） | 约 1.8 MiB | 可忽略 |
>
> 单位说明：「MB」= 10⁶ 字节，「MiB / `du` 显示的 M」= 2²⁰ 字节。
> 一句话：**压缩包 17M，解开后 27M —— 其中 22M 是漫画、4.2M 是课程图谱**。
> （漫画是从 PDF 里原样抽出的 JPEG，压缩率约 27%，所以压缩包比落盘小得多，不是文件缺失。）

**不需要 `pip install`**：`requirements.txt` 与上一版逐字节相同 —— 本次没有引入任何新的运行时 Python 依赖。

> ### ⚠️ 本次**必须清一次 `backend/cache/`**（唯一必须做的手工步骤）
>
> 投资学 / 公司金融 / 并购与重组这三个 `graph_id` 的**图谱数据被整体换掉了**
> （旧分层图 → 新版 6 层课程图谱）。`backend/cache/<graph_id>_all-MiniLM-L6-v2/` 里存的是**旧图的向量**，
> 只要文件还在，程序就会继续吃旧缓存 → 问答会引用到「新图里已不存在的节点」。
>
> ```
> 停服后删除（整目录删即可，程序会自动重建）：
>     backend/cache/invest_all-MiniLM-L6-v2/
>     backend/cache/corp_fin_all-MiniLM-L6-v2/
>     backend/cache/ma_all-MiniLM-L6-v2/
> 或者干脆：rm -rf backend/cache/   （Windows: rmdir /s /q backend\cache）
> ```
>
> 重建会在启动预热的**后台任务**里自动完成（一次性，见第 3 节耗时提示）。

---

## 1. 压缩包怎么用

- 包内**按原目录结构**保存了「新增」与「有改动」的文件（相对项目根目录的路径与服务器上一致）。
- 用法：**解压后整体覆盖到服务器项目根目录**，保留目录结构。
- 包内附 `更新清单.txt`：逐文件列出「新增/替换」、体积、SHA256，可用它核对覆盖结果。
- 包内附 `更新说明.md`：本文件的副本。

包内**不含**下列内容（都不需要更新，也不用重传）：

| 未包含 | 原因 |
|---|---|
| `.git/`、`.workbuddy/` | 版本库 / 本地工作目录，与运行无关 |
| `data/media/textbooks/`（32 个文件 / 494.2 MiB） | **上一版已交付，本次逐字节未改动**，不用重传 |
| `data/media/textbooks.json` | 同上，未改动 |
| `data/media/books/tb-*.jpg`（28 张真封面） | 同上，未改动 |
| `data/media/books/01–35.png`（35 张） | 上一版的占位封面，**未改动**（页面已不引用，见第 6 节） |
| `书/`（根目录，约 495M） | 教材的源素材目录，服务器上不需要；内容与 `data/media/textbooks/` 逐字节相同 |
| `知识点小漫画/`（根目录，约 28MB / 3 个 PDF） | **漫画的源素材目录**，服务器上不需要；抽出的图已在包内 `data/media/comics/` |
| `数据/`（约 4.8 GB） | 原始交付素材，gitignore，不进任何交付包 |
| `data/media/videos/`、`papers/`、`videos.json` | 与上一版**逐字节相同**，无需重传 |
| `data/layered/{invest,corp_fin,ma}_layered.json` 等旧图 | 这三个 graph_id 已不再读它们；文件留着无害（见第 6 节），也**不需要重传** |
| `前端/SUFE-Knowledge-Galaxy/data/galaxy_v12.json` | 上一版的可选本地兜底副本，未改动，不用重传 |
| `backend/cache/` | 运行期 embedding 缓存，服务器上会自动重建（**而且本次必须清掉**，见第 0 节） |
| `前端/proxy-server/node_modules/` | 本地依赖，服务器不需要 |
| `文档/交接/*` | 发给**前端负责人**的对接文档，与服务器部署无关（本次说明文档单独在本表之外，包内已带副本） |

---

## 2. 改了什么（逐文件）

### 2.1 后端 Python（6 改 + 1 新增）

| 文件 | 类型 | 改动内容 |
|---|---|---|
| `backend/service/data_service.py` | **替换**（关键） | ① `GRAPH_FILES` 里 `invest` / `ma` / `corp_fin` 三个 id 指向 `data/media/graphs/course_graph_*.json`（其余 4 个 id 不动）；② `_load_file()` 归一：`layer ← layer 或 level`、`media` 原样透传、其余原始字段进 `extra`（前端靠这个读 `level`/`parent_id`/`description`）。旧图路径写在注释里，随时可回退 |
| `backend/controller/router.py` | **替换**（关键） | 新增 `CHAPTER_LAYER = {invest:'meso', corp_fin:'meso', ma:'meso', intl_inv:'macro'}`，`/api/graph/book/{id}` 不再写死取 `macro` 层。**新图的 `macro` 只有 1 个课程根**，不改这行书详情小图会退化（书 1 由 28 章变 1 个点） |
| `backend/controller/media.py` | **替换** | ① `MEDIA_MOUNTS` 新增 `"/assets/comics": "comics"`；② 新增 `GET /data/comics.json`（漫画清单）与 `GET /api/comics`（套统一外壳）。其余路由一字未动 |
| `backend/controller/graph_ids.py` | 替换 | `GRAPH_FILES` 同步（**仅文档性质**，控制层不读文件，保留是为了两边一致） |
| `backend/tests/test_data_service.py` | **新增** | 数据层新单测：`name→label` / `level→layer`、`parent_id`/`description` 进 `extra`、`media` 透传且不进 `extra`、`get_layer` 取章层、边原样、关键词检索能命中 `description` |
| `backend/tests/test_media_api.py` | 替换 | 新增 3 条漫画测试（图片可取、清单 200、`/api/comics` 外壳），并把缺失清单 404 用例扩了两条。**不改测试也能跑** |
| `backend/tests/test_router.py` | 替换 | `test_book_graph_returns_macro_layer` → `test_book_graph_returns_chapter_layer`，断言 `CHAPTER_LAYER` 四个值 + 书 1 取 meso / 书 3 取 macro |

> **没有改** `main.py`、`backend/agent/`、`backend/src/`。
> 启动预热的图谱仍是 7 个：`ma / corp_fin / intl_inv / invest / econ / v6 / v12`。

### 2.2 前端 `前端/SUFE-Knowledge-Galaxy/`（11 改 + 4 新增）

**替换**（这些都是「前端最终版」的内容，上一版没发出去）：

| 文件 | 说明 |
|---|---|
| `index.html` | 新增 `css/database-board.css`、`css/comic-reader.css` 与对应 script；漫画阅读遮罩 `#comicMask`（**script 必须排在 `page-slider.js` 之前**）；导航新增「数据库」入口 |
| `css/style.css`、`css/page-slider.css`、`css/knowledge-galaxy.css`、`css/course-galaxy.css` | 前端最终版样式（行情侧栏三日/单日 + 成交量、课程图谱切换、整体升级） |
| `js/main.js` | 最终版逻辑（数据库板块、行情侧栏）—— 上一版的教材部分（`BOOKS` 32 本 / 「📖 阅读原文」）**全部保留** |
| `js/knowledge-galaxy.js`、`js/course-galaxy.js`、`js/galaxy.js`、`js/page-slider.js`、`js/assistant.js` | 前端最终版引擎与交互 |

**新增**：

| 文件 | 体积 | 说明 |
|---|---|---|
| `js/comic-reader.js` | 15,133 字节 | 漫画阅读器。对外**只暴露** `window.ComicReader = {open, close, peek}` |
| `css/comic-reader.css` | 6,610 字节 | 详情面板入口卡片 + 全屏翻页遮罩（含夜间主题） |
| `js/database-board.js` | 27,022 字节 | 「数据库」板块 |
| `css/database-board.css` | 17,779 字节 | 同上 |

> **漫画那 2 个文件是「外挂式」的**：把 `index.html` 里的那 2 行引用（1 个 css + 1 个 script）连同 `#comicMask` 一并删掉，
> 漫画功能立刻完全消失、其余一切照旧。方便出问题时快速隔离。

### 2.3 数据文件（**最关键：缺了漫画就全 404**）

| 文件 | 体积 | 用途 |
|---|---|---|
| `data/media/comics/` | **新增**，共 **144 个文件 / 22,418,451 字节 / 21.38 MiB**（3 个子目录 `inv/` `brealey/` `intl/`，24 部 × 6 页） | 漫画图片。URL 前缀 `/assets/comics/` |
| `data/media/comics.json` | **新增**，37,926 字节 | 漫画清单：24 部 × 6 页的 `id/course/course_label/title/dir/files/pages` |
| `data/media/graphs/course_graph_investment.json` | **新增**，2,474,578 字节 | **投资学新版 6 层课程图谱**（2583 节点 / 4175 边 / 28 章），也是 `/api/graph/load` 的数据源 |
| `data/media/graphs/course_graph_mergers.json` | **新增**，551,035 字节 | 并购与重组（611 节点 / 8 章） |
| `data/media/graphs/course_graph_corporate_finance.json` | **新增**，1,283,029 字节 | 公司金融（1221 节点 / 21 章） |
| `data/media/databases.json` | **新增**，4,513 字节 | 「数据库」板块的表格数据（前端按原路径 `/data/databases.json` 取） |
| `data/layered/intl_inv_layered.json` | **替换**，878,132 字节 | 国际投资学仍走旧分层图，本次只是给它挂上漫画（内容除漫画字段外零差异） |

> ⚠️ `data/media/comics/` **必须完整落位**（144 个文件 / 21.38 MiB）。
> 少放一个，那一页漫画就 404。核对方法见第 3 节第 4 步。
>
> ⚠️ 那 3 个 `course_graph_*.json` 有**两个用途**，缺一不可：
> ① 后端 `data_service` 读它们当 `/api/graph/load` 的数据源；
> ② 站点根路由 `/<文件名>` 直接对外提供（前端在接口失败时的本地兜底）。

### 2.4 脚本（留档，服务器上不用跑）

| 文件 | 说明 |
|---|---|
| `scripts/import_comics.py` | 从根目录 `知识点小漫画/` 的三个 PDF 里**无损抽取内嵌 JPEG** → `data/media/comics/`，并生成 `data/media/comics.json` |
| `scripts/link_comics.py` | 按「章」粒度把漫画写进图谱节点的 `media.comic` 字段（幂等，可反复跑） |

### 2.5 文档（跟包一起发，供查）

| 文件 | 说明 |
|---|---|
| `文档/记录/前端改动记录.md` | 前端改动逐条台账（本次新增 2026-09-24 一节：漫画接入） |
| `文档/设计/数据层分层设计.md` | 新增第八节：**现在有两套图谱并存**（旧分层图 / 新版课程图）的字段与层级差异、`CHAPTER_LAYER`、回退方式 |
| `更新说明.md` | 本文件的副本，**放在压缩包根目录**，解压后直接看得到 |

> ### ⚠️ 关于漫画入口（页面看着不对时先看这里）
>
> - **入口只在「知识点详情面板」里**，**不在**课程板块单开一个漫画列表。
> - **必须先钻到「知识点」这一级**：课程星系里点**章**、点**节**都只下钻、**不弹面板**；
>   点到最末一级的**知识点**（节点文字如「投资的定义」）才弹出右侧详情面板，
>   面板里「收藏 / 写笔记」下方就是 **「🎨 看图解漫画」**。
> - 按钮右侧会写清是哪一部哪一页，例如 `第 1 部 · 绪论：什么是投资｜实物资产与金融资产（投资的定义）`。
> - 阅读器里：`←/→` 翻页、`Home/End` 首末页、`Esc` 关闭，底部有 6 张缩略图可直接跳。
>   **在阅读器里按 ←/→ 不会让背后的页面翻页**（特意做了键盘拦截）。
> - 「并购与重组」这门课**没有漫画**（用户给的素材里就没有），点它的知识点**不出现按钮**，属**预期**。
> - 投资学的「证券收益实证证据」、公司金融的「债券与股票估值」两**章**暂无漫画，亦属预期。
>
> ### ⚠️ 老坑复习：前端资源一律不准外链 CDN（本次已按此原则检查）
>
> 更早的版本曾出现「首页指数表 0 行、走势图空白」，根因是 `index.html` 外链 `cdn.jsdelivr.net` 的 Chart.js，
> 国内网络 `ERR_CONNECTION_RESET` → `window.Chart` 未定义 → `main.js` 的 `initChart()` 抛错 →
> 后面的行情/新闻/职位初始化全部不执行。
> **本次新前端经实测：`index.html` 里 0 条外链，`chart.min.js` / `d3.min.js` / `jszip` / `epub` 全部是本地文件。**
> （`js/galaxy.js`、`js/course-galaxy.js` 里各留了一份 d3 的 CDN 兜底域名列表，但 `index.html` 已先加载本地 `js/d3.min.js`，
> 那份列表永远不会被用到 —— 属历史遗留，不影响。）
> 自检时确认 `curl -o /dev/null -w '%{http_code}' http://<服务器>:8000/js/chart.min.js` 返回 **200**。

---

## 3. 更新步骤

```
1) 停后端（Ctrl+C 停掉 uvicorn 进程）

2) 备份（重要，便于回退）：
     - 前端/SUFE-Knowledge-Galaxy/        整个目录
     - backend/service/data_service.py    单文件
     - backend/controller/router.py       单文件
     - backend/controller/media.py        单文件
     - data/layered/intl_inv_layered.json 单文件
     - backend/cache/                     整个目录（本次要清，先备份更稳）
     （若服务器接了 git，直接 commit 一次更省事）

3) 解压 部署更新包_2026-09-24.zip，覆盖到项目根目录（保持目录结构）
   —— 包约 17 MiB，解开后约 27 MiB，主要落在 data/media/comics/（22 MiB），很快

4) ★ 清掉三个过期向量缓存（本次唯一必须做的手工步骤，见第 0 节说明）：
     Linux  ：  rm -rf backend/cache/invest_all-MiniLM-L6-v2 \
                       backend/cache/corp_fin_all-MiniLM-L6-v2 \
                       backend/cache/ma_all-MiniLM-L6-v2
     Windows：  rmdir /s /q backend\cache\invest_all-MiniLM-L6-v2
                rmdir /s /q backend\cache\corp_fin_all-MiniLM-L6-v2
                rmdir /s /q backend\cache\ma_all-MiniLM-L6-v2

5) 核对新文件（最容易出问题的一步）：
     a) 漫画图片个数必须是 144：
        Linux  ：  find data/media/comics -type f | wc -l        → 144
        Windows：  dir /s /b data\media\comics\*.jpg | find /c /v ""
     b) 漫画目录体积必须是 22,418,451 字节（= 21.38 MiB）：
        Linux  ：  du -sh data/media/comics                     → 22M
        Windows：  (Get-ChildItem data\media\comics -Recurse -File | Measure-Object Length -Sum).Sum
                                                                → 22418451
     c) 课程图谱 3 个文件都在：
        Linux  ：  ls -l data/media/graphs/                     → course_graph_{investment,mergers,corporate_finance}.json
     d) 抽一个漫画文件对 SHA256（值见 更新清单.txt）：
        Windows：  certutil -hashfile data\media\comics\inv\inv_p01_2.jpg SHA256
        Linux  ：  sha256sum data/media/comics/inv/inv_p01_2.jpg

6) 启动后端：
     Windows：  E:/python/python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000
     Linux：    python -m uvicorn main:app --host 0.0.0.0 --port 8000

7) 看启动日志，等到出现「[preload] xxx 预热完成」共 7 行：
     ma / corp_fin / intl_inv / invest / econ / v6 / v12
```

### 首次启动耗时与内存提示

- 预热在**后台任务**里做，这期间**页面可以正常打开**，只是知识星系板块要等预热完才有点击/问答响应。
- **本机按第 3 节第 4 步清掉那三份缓存后实测（真·冷启动，7 个图谱全部预热）**：

  | 时点 | 事件 |
  |---|---|
  | `14:21:34.4` | `已在后台启动 7 个图谱的 embedding 预热任务` |
  | `14:21:36.4` | `[preload] ma 预热完成（611 节点 / 884 边）` |
  | `14:21:39.8` | `[preload] corp_fin 预热完成（1221 节点 / 2256 边）` |
  | `14:21:41.1` | `[preload] intl_inv 预热完成（913 节点 / 3660 边）` |
  | `14:21:45.5` | `[preload] invest 预热完成（2583 节点 / 4175 边）` |
  | `14:21:46.9` | `[preload] econ 预热完成（2452 节点 / 7718 边）` |
  | `14:21:48.3` | `[preload] v6 预热完成（3661 节点 / 6058 边）` |
  | `14:21:51.6` | `[preload] v12 预热完成（31429 节点 / 55997 边）` |

  **7 行全部出齐总耗时约 17 秒**（从 34.4s 到 51.6s）。本次要重建的三份分别是
  `ma` ≈2.0s、`corp_fin` ≈3.3s、`invest` ≈4.3s（各自含建图 + embedding）。
  重建出来的缓存体积约：`invest` 20M、`corp_fin` 11M、`ma` 4.4M。
- **内存**：新图的 `invest` 节点数从 331 涨到 2583（+7.8 倍），embedding 常驻内存会相应上升。
  本机实测进程常驻约 **1.5 GB**（1,581,316 K）。漫画是纯静态文件、按需读取，**不占常驻内存、不增加启动耗时**。

### 本次不需要动别的缓存

- `intl_inv` 的数据只是多了漫画字段（节点/边一个没变），**缓存不用清**（清了也无害）。
- `econ` / `v6` / `v12` 完全没动。

---

## 4. 更新后自检（约 3 分钟）

### 4.1 命令行自检

```bash
# 1) 服务活着
curl -s http://127.0.0.1:8000/health
# 期望 → {"status":"ok","agent":true}

# 2) 漫画清单：必须是 total_albums=24 / total_pages=144
#    注意：这个 JSON 是缩进格式，别用 head 截，用 grep 取字段最稳
curl -s http://127.0.0.1:8000/data/comics.json | grep -E '"source_dir"|"total_albums"|"total_pages"'
# 期望 →
#   "source_dir": "知识点小漫画/",
#   "total_albums": 24,
#   "total_pages": 144,

# 3) 漫画图片能取到（随抽两门课）
curl -s -o /dev/null -w '%{http_code} %{size_download}\n' \
     http://127.0.0.1:8000/assets/comics/inv/inv_p01_2.jpg
# 期望 → 200 176776
curl -s -o /dev/null -w '%{http_code} %{size_download}\n' \
     http://127.0.0.1:8000/assets/comics/brealey/brealey_p01_2.jpg
# 期望 → 200 152896

# 4) 统一外壳接口（给前端以后切 API 留的）
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8000/api/comics
# 期望 → 200

# 5) 课程图谱数据（既是 load 数据源，也是站点根路由）
for u in /course_graph_investment.json /course_graph_mergers.json \
         /course_graph_corporate_finance.json /data/databases.json; do
  printf '%-42s ' "$u"
  curl -s -o /dev/null -w '%{http_code} %{size_download}\n' "http://127.0.0.1:8000$u"
done
# 期望 → 200 2474578 / 200 551035 / 200 1283029 / 200 4513

# 6) 新增前端资源（必须全部 200）
for u in /css/comic-reader.css /js/comic-reader.js /css/database-board.css /js/database-board.js; do
  printf '%-30s ' "$u"
  curl -s -o /dev/null -w '%{http_code} %{size_download}\n' "http://127.0.0.1:8000$u"
done
# 期望 → 200 6610 / 200 15133 / 200 17779 / 200 27022

# 7) ★ 回归：课程图谱接口已换新图（投资学应为 2583 节点 / 4175 边）
curl -s -X POST http://127.0.0.1:8000/api/graph/load \
     -H 'Content-Type: application/json' -d '{"graph_id":"invest"}'
# 期望 → "code":0，nodes 2,583 / edges 4,175（旧图只有 331 节点，一眼能看出换没换）

# 8) ★ 回归：书详情小图取的是「章」层（书 1 = 投资学，应为 28 章）
curl -s http://127.0.0.1:8000/api/graph/book/1 | grep -c '"id"'
# 期望 → 至少 28（旧版写死 macro 只会返回 1 个课程根）

# 9) 回归：老资源没坏
for u in /js/chart.min.js /data/textbooks.json /data/videos.json /data/courses.json; do
  printf '%-30s ' "$u"
  curl -s -o /dev/null -w '%{http_code}\n' "http://127.0.0.1:8000$u"
done
# 期望 → 200 / 200 / 200 / 200

# 10) 既有的大图（v12）没被影响
curl -s -X POST http://127.0.0.1:8000/api/graph/load \
     -H 'Content-Type: application/json' -d '{"graph_id":"v12"}'
# 期望 → "code":0，nodes 31,429 / edges 55,997

# 11) 后端单测
cd backend && python -m pytest -q
# 期望 → 135 passed
```

### 4.2 浏览器自检

打开 `http://<服务器>:8000/index.html`，按顺序看：

1. 顶部导航应有 **9 项**，其中 **「数据库」** 是本次新增的板块；点进去能看到 3 张表（国际市场 / 中国市场 / 免费公开数据），**共 34 个数据源**，并能切「阅读」态看仿 Excel 工作簿 / 导出 CSV。
2. **首页行情**：右侧指数侧栏有数据；走势图有曲线；切「三日 / 单日」图表跟着变；成交量柱在下方。（若这里空白，先查 `js/chart.min.js` 是否 200。）
3. 进 **「课程星系」**：
   - 顶部三个选项卡「投资学 / 并购与重组 / 公司金融」，默认「投资学」。
   - 左侧「主题目录」应列出 **28 个章**（不是旧版的 11 个）。
   - 点一个**章** → 只下钻、不弹面板；点一个**节** → 继续下钻；点到**知识点**（如「投资的定义」）→ **右侧弹出详情面板**。
4. **漫画**：在上面那个知识点面板里，看到 **「🎨 看图解漫画」** → 点它 → 全屏打开漫画，页码显示 `2 / 6`，
   底部 6 张缩略图；按 `←/→` 翻页、`Home/End` 跳首末页、`Esc` 关闭。
   **重点验证**：在漫画里按 `←/→`，**背后的站点页面不能翻页**；`Esc` 关掉后再按 `→`，页面翻页功能恢复正常。
5. 回到「课程」板块（教材）：书架仍是 **32 本**，点封面 → 详情 → 「📖 阅读原文」照旧能读（**本次没动这块**）。
6. 顺手回归：知识星系（v12）能出图、视频能播、论文 PDF 能开。

### 4.3 本机参考基线（2026-09-24 实测）

| 项目 | 结果 |
|---|---|
| 后端单测 `pytest -q` | **135 passed**（上一版 123 + 本次新增/扩充 12） |
| `/health` | `{"status":"ok","agent":true}` |
| `/data/comics.json` | **200**，37,926 字节，`total_albums=24` / `total_pages=144` |
| `/api/comics` | **200**，23,210 字节 |
| `/assets/comics/inv/inv_p01_2.jpg` | **200**，176,776 字节，`image/jpeg` |
| `/assets/comics/brealey/brealey_p01_2.jpg` | **200**，152,896 字节 |
| `/data/databases.json` | **200**，4,513 字节 |
| `/course_graph_investment.json` / `mergers` / `corporate_finance` | **200**，2,474,578 / 551,035 / 1,283,029 字节 |
| `/css/comic-reader.css` / `/js/comic-reader.js` | **200**，6,610 / 15,133 字节 |
| `/css/database-board.css` / `/js/database-board.js` | **200**，17,779 / 27,022 字节 |
| `/js/chart.min.js` | 200（205,086 字节，未改动） |
| `/data/textbooks.json` | 200（22,625 字节，未改动） |
| `graph_id=invest`（新图） | **2,583 节点 / 4,175 边**（旧图 331 节点） |
| `graph_id=ma` / `corp_fin`（新图） | 611 节点 / 8 章；1,221 节点 / 21 章 |
| `/api/graph/book/1`（投资学书详情小图） | **28 章**（旧版写死 macro 只有 1 个根） |
| `graph_id=v12` | 31,429 节点 / 55,997 边（未受影响） |
| 无头浏览器端到端（真实页面 + 真实后端） | **28/28 全绿**：课程星系 = 新版 6 层图（2583 + 1 虚拟域根 / 28 章）；点「投资的定义」→ 面板出现「🎨 看图解漫画」；点开 → 遮罩打开、图片 `naturalWidth=1024`、`2 / 6`、6 张缩略图；`←/→` 只翻漫画且当前屏不变、`Home/End` 首末页并正确置灰；`Esc` 关闭后 `→` 恢复正常翻页；**无漫画的节点按钮正确隐藏**（负向用例）；**JS 异常 0**；漫画资源 0 个失败请求（仅既有 `favicon.ico` 404 与视频板块预取中止的噪声） |

---

## 5. 关于 `data/media/`（大体积数据）

- 本次**包含** `data/media/comics/`（144 个文件 / 22,418,451 字节 / 21.38 MiB）+ `data/media/graphs/course_graph_*.json`（3 个 / 4.11 MiB）——**必须完整落位**。
- 本次**不包含**、也**不需要重传** `textbooks/`、`books/`、`videos/`、`papers/`、`videos.json`、`textbooks.json`、`galaxy_v12.json`（均与上一版逐字节相同）。
- 若服务器上 `data/media/` **整个目录都不存在**（表现：首页视频无法播放、论文 PDF 404、封面空白、漫画 404），
  需要把媒体数据补齐。更新后的完整目录结构：

  ```
  data/media/
    videos/           ← 80 个 mp4 + 80 张封面（约 4.8 GB）
    papers/           ← 13 个 PDF（约 17 MB）
    books/            ← 旧的 01–35.png（35 张） + tb-*.jpg（28 张真封面）
    textbooks/        ← 32 个 PDF/EPUB（494.2 MiB）
    comics/           ← 【本次新增】144 个 jpg（22,418,451 字节 = 21.38 MiB）
    graphs/           ← 【本次新增】course_graph_{investment,mergers,corporate_finance}.json（4.11 MiB）
    videos.json       ← 视频清单
    courses.json      ← 课程星系数据
    textbooks.json    ← 教材清单
    comics.json       ← 【本次新增】漫画清单（37,926 字节）
    databases.json    ← 【本次新增】「数据库」板块数据（4,513 字节）
  ```

  补齐方式见项目内《测试环境说明.md》与对应 `scripts/import_*.py`。
- 这些路径由后端 `backend/controller/media.py` 按**前端原有相对路径**对外提供
  （`/assets/comics/inv/inv_p01_2.jpg`、`/data/comics.json`、`/data/databases.json` 等），
  前端代码里写的就是这些地址，所以只要文件放对位置，**前端不用改任何一行**。

---

## 6. 可以顺手清理的东西（可选，不影响功能）

| 位置 | 说明 |
|---|---|
| `data/layered/{invest,corp_fin,ma}_layered.json` | 这三个 id 已改用新版课程图，旧分层文件**不再被读取**。留着无害（约 1.5 MB），确认无用后可删 |
| `data/media/books/01–35.png`（35 张，约 5.5 MiB） | 上一版之前的占位封面，页面已全部改用 `tb-*.jpg`，**不再被引用** |
| 根目录 `书/`（约 495M） | 教材源目录，**服务器上完全不需要**（运行只读 `data/media/textbooks/`） |
| 根目录 `知识点小漫画/`（约 28MB，3 个 PDF） | 漫画源素材目录，**服务器上完全不需要**（运行只读 `data/media/comics/`） |
| `前端/proxy-server/` | 行情代理服务，早已由后端 `/api/kline` 接手。若还在可整目录删除 |
| 项目根目录的 `SUFE-Knowledge-Galaxy/` | 最早期整包交付副本（约 19 MB）。当前后端只托管 `前端/SUFE-Knowledge-Galaxy/` |

> 小提示：`前端/SUFE-Knowledge-Galaxy/启动网页.bat` 里仍写着 `http://localhost:3000/index.html`，
> 这是前端交付包自带的历史文件（属前端文件，我们未改动）。服务器上用不上它 —— 请统一用后端 8000 端口访问。

---

## 7. 回退方式

覆盖前若按第 3 节备份了那 5 处（前端目录、3 个后端文件、1 个数据文件），
回退时还原它们 + 重启后端即可。**本次没有改数据库结构，也没有引入需要迁移的持久化格式。**

⚠️ **回退后一定要再清一次缓存**（同样三个目录）：因为回退把 `data_service.py` 换回旧图数据源时，
缓存里已经是**新图的向量**了 —— 反向同理，不清就会错配。

新增的 `data/media/comics/`、`data/media/comics.json`、`data/media/graphs/course_graph_*.json`、
`data/media/databases.json` 不回退也**完全无害** —— 旧版前端不认识它们，只会当普通静态文件忽略。

**数据文件说明（不会丢）**：

- 用户账号在 `data/users.json`，个人笔记/收藏在 `data/profiles.json`，**重启后端不会丢**。
- 重启后会清空的只有**进程内的登录令牌与会话**（设计如此，用户需重新登录）。
- `data/layered/` 里除 `intl_inv_layered.json`（只多了漫画字段）外，其余 6 个图谱**一字未动**。

---

## 8. 附：本次改动的范围界定

- 服务器当前状态 = 版本库提交 **`d9fc961`**（2026-09-22 交付）**+ `部署更新包_2026-09-23.zip` 覆盖后**。
- 本次更新 = **上述状态 → 当前工作区**。包内共 **179 个条目**（`更新清单.txt` 里的 178 条 + `更新说明.md` 本身）：

  | 类别 | 个数 | 文件 |
  |---|---|---|
  | **替换** | **18** | 后端 6：`service/data_service.py`、`controller/{router,media,graph_ids}.py`、`tests/{test_media_api,test_router}.py`；数据 1：`data/layered/intl_inv_layered.json`；前端 11：`index.html` + 4 个 css + 6 个 js |
  | 替换（可选） | 3 | `.gitignore`、`文档/记录/前端改动记录.md`、`文档/设计/数据层分层设计.md` —— 不影响功能，可跳过 |
  | **新增** | **154** | 漫画 145（`data/media/comics/` 144 张 + `data/media/comics.json`）+ 课程图谱 3（`data/media/graphs/course_graph_*.json`）+ `data/media/databases.json` + 前端 4（`js/comic-reader.js`、`css/comic-reader.css`、`js/database-board.js`、`css/database-board.css`）+ 后端测试 1（`tests/test_data_service.py`） |
  | 新增（可选） | 2 | `scripts/import_comics.py`、`scripts/link_comics.py` —— 服务器上不用跑，留档 |
  | 说明 | 1 | `更新说明.md`（本文件的副本，放在包根目录） |

  逐文件的体积与 SHA256 见包内 `更新清单.txt`（也在 `部署包/更新清单.txt`）。
- 本次增量是**按「服务器当前内容的逐文件指纹」**算出来的，不是凭印象列的文件：
  以 `部署包/更新清单_2026-09-23.txt` 的 SHA256 为「上一版覆盖后」的指纹，
  其余文件则与提交 `d9fc961` 的文件树逐字节比对；文本文件按 **LF 归一等价**比较，规避 CRLF/LF 假差异。
  两个提交（`f718504` 前端最终版合并、`bb1fc45` 漫画接入）之间**未改动**的文件**一律不进包**（共 110 个）。
- 服务端代码与数据由我们（控制层/后端）负责；**Agent 层（`backend/agent/`、`backend/src/`）本次未改动**。
- 漫画与教材电子版均由用户提供（分别放在根目录 `知识点小漫画/` 与 `书/`），我们只做导入、挂载、清单生成与前端接入。

如有任何一步对不上，请把启动日志（尤其是 `[preload]` 那几行）与浏览器 Console 的报错发回，我们按日志定位。
